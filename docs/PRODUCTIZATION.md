# Productization record

Last updated: 2026-09-01

## Current v0.15 engineering status

The bounded inbox-state slice starts from clean synchronized `main` at merged PR #62 (`aaf828f`) and its successful 19-job exact-main run: **724 Python tests, two skips, 90.10% coverage**, 123 live PostgreSQL tests, 62 SDK tests, package/wheel, container, restore/PITR, five load profiles and Kubernetes gates. Current Stream documentation calls channel lists a core messaging view and permits unread sorting/filtering; Sendbird exposes per-channel and aggregate unread counts; Twilio pairs each user-conversation unread count with its last-read cursor. Those contracts support an inbox need, not a reason to import hosted-platform membership into Samsarix.

Samsarix therefore accepts an explicit ordered set of 1–100 room IDs from the host application at `POST /v1/read-states/query`. The signed token must grant `room:read` to every ID before storage lookup. One SQLite or PostgreSQL statement returns cursor/unread state, latest non-deleted retained message ID/time, total unread messages and unread-room count. Any unauthorized, missing or actively banned room fails the whole request; the response contains no body, sender, metadata, attachment, reaction or pin data and creates no cursor rows. The host retains accounts, membership, assignment, labels, previews, pagination and notification policy.

The request bound, content-free result and dedicated per-subject 60-second allowance constrain amplification. SQLite applies the allowance per supported single process. PostgreSQL uses its deployment-wide rate buckets with a distinct key prefix in the existing bounded-query namespace, avoiding a schema change or collision with message-search budgets. Unpublished TypeScript SDK 0.10.0 validates the same room count, syntax and uniqueness before transport and exposes `queryReadStates()`.

Final local verification passes Ruff lint/format, mypy, compileall, **609 non-PostgreSQL tests with two expected skips and 125 live PostgreSQL tests deselected**; full local collection reports **619 passed and 117 environment/platform skips**. The strengthened eight-case inbox API suite, **89 rate/runtime/workflow tests with three expected PostgreSQL skips**, and all **63 TypeScript SDK tests** plus declaration build/package inspection also pass. The real TypeScript HTTP/WebSocket journey reports `inbox=ok`. A clean sdist/wheel passes Twine. A fresh Python 3.14 wheel-only environment loads version 0.12.0 from `site-packages`, passes `pip check`, reports no known vulnerabilities in published dependencies, and completes the installed HTTP/WebSocket/application smoke with `inbox=ok`. The workstation-global dependency audit cannot enumerate one unrelated distribution with missing version metadata, so it is not represented as a passing signal; the isolated runtime audit is the reproducible result.

A sealed Codex Security diff scan reviewed all 11 changed source-bearing files plus supporting authorization, rate-limit and regression paths. It found **zero reportable findings** across cross-room authorization, SQL construction, SQLite/PostgreSQL subject isolation, tombstone handling, state-mutation, amplification and SDK/package exposure. The review was sequential because this desktop session forbids delegation; TAC advisory status was unavailable because its connector was disconnected.

Implementation head `7ec182c` then passed all 19 jobs in [exact-head run 33481979085](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33481979085): **734 Python tests with two skips and 90.21% branch-inclusive coverage**, all **125 live PostgreSQL tests**, 63 SDK tests, dependency audits, package and installed-wheel smoke, container, Kubernetes, logical restore, physical PITR and five strict load/recovery profiles. Attempt 1 preserved one existing retained-gap recovery failure after fencing/recovery had succeeded: the deployment-wide WebSocket count did not settle to one inside its 10-second observation window; the dedicated retained-gap load profile and the other 124 PostgreSQL tests passed. The unchanged-sha attempt 2 reran the complete PostgreSQL marker set and passed all 125 cases in 267.72 seconds. This is accepted as runner-timing variance rather than erased evidence; a repeat on the documentation head remains a merge blocker. Protected merge and exact-main CI remain gates for this slice.

Primary references checked on 2026-09-01: [Stream channel queries](https://getstream.io/chat/docs/javascript/query-channels/), [Sendbird unread totals](https://sendbird.com/docs/chat/platform-api/v3/user/managing-unread-count/get-number-of-unread-items), [Sendbird channel representation](https://sendbird.com/docs/chat/platform-api/v3/channel/channel-overview), and [Twilio user conversations](https://www.twilio.com/docs/conversations/api/user-conversation-resource). These references establish familiar integration shape, not demand or product-market fit.

## Previous v0.14 engineering status

The host-owned attachment-reference slice starts from clean synchronized `main` at merged PR #61 (`22fa265`) and its successful 19-job exact-main run: **708 Python tests, two skips, 90.06% coverage**, 121 live PostgreSQL tests, 61 SDK tests, package/wheel, container, restore/PITR, load and Kubernetes gates. Current Sendbird and Stream contracts accept externally hosted file URLs, while OWASP recommends isolating upload storage and applying authorization, size, type and scanning controls; Google Cloud and Azure explicitly treat signed object URLs/tokens as bearer credentials. Samsarix therefore stores neither blobs nor URLs: it accepts at most five ordered opaque descriptors, rejects unknown fields and duplicate IDs, and caps their canonical JSON at 8192 bytes.

HTTP and WebSocket creation accept attachment-only messages and return descriptors through history, replies, search, pins, export schema 7, realtime events, and selected signed webhooks. Content/metadata edits preserve immutable references. SQLite schema 10 and PostgreSQL schema 13 persist default-empty arrays; tombstones clear them, retained PostgreSQL message events scrub them, and the existing webhook deletion boundary removes prior payload bodies. Unpublished TypeScript SDK 0.9.0 validates outbound descriptors and keeps inbound attachment fields optional for released-0.12 compatibility.

The host application remains responsible for authenticated upload initiation, extension/signature/type validation, malware or content scanning, quotas, isolated object storage, fresh room/user authorization on every download, short-lived signed URL issuance, orphan cleanup, retention, and storage/egress cost. Descriptor IDs are references, never authority. The engine does not fetch or delete objects and deliberately provides no multipart upload endpoint, proxy download, media transformation, signed URL, storage SDK, or false content-safety claim.

Final local verification passes Ruff lint/format, mypy, compileall, **599 non-PostgreSQL tests with two expected skips and 123 live PostgreSQL tests deselected**, and all **62 TypeScript SDK tests**. The 31-file TypeScript package passes inspection and its real HTTP/WebSocket/reconnect journey reports `attachments=ok`. A clean sdist/wheel passes Twine; a fresh Python 3.14 wheel-only environment passes `pip check`, reports no known third-party vulnerabilities, and completes the installed HTTP/WebSocket/webhook/export/lifecycle/backup smoke with attachment creation, preservation, and scrubbing.

Exact-PR verification preserved three instructive red heads. [Run 33475041204](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33475041204) exposed a missing instance lease in the new retained-event test rather than a product failure. Its correction passed all 123 live PostgreSQL cases, but retained-gap in [run 33475479741](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33475479741) twice missed exactly one of 3600 open-arrival slots during the dense recovery replay. The strict gate was not relaxed: the bounded load driver now collects once, disables only cyclic garbage collection during scheduled arrivals, restores its prior state before reconciliation, and records that behavior. [Run 33476928932](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33476928932) then started all 3600 slots but exposed one lifecycle-boundary accounting race; a request entering just before the intentional room transition could receive its expected 409 just after the control flag changed. Regression coverage permits only an affected room's expected frozen/archived code when a request overlaps either window edge; unrelated rooms and errors still fail.

The corrected implementation and harness pass all 19 jobs in [exact-head run 33477356063](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33477356063): **724 Python tests, two skips, 90.13% branch-inclusive coverage; all 123 live PostgreSQL tests; 62 SDK tests; clean dependency audit; package metadata and installed-wheel attachment smoke; container, Kubernetes, logical restore and physical PITR acceptance; and all five strict 3600-cycle load/recovery profiles**. Retained-gap starts every offered cycle, converges all 32 clients, verifies all 32 authorization denials, drains every lease and reconnects in 1.022 seconds on that shared runner. These are bounded CI observations, not production capacity or availability claims. Protected merge and exact-main CI remain gates. No registry publication, hosted object, production deployment, customer data, telemetry, paid service, or owner infrastructure is introduced.

Primary references checked on 2026-09-01: [Sendbird external file-message URL](https://sendbird.com/docs/chat/platform-api/v3/message/messaging-basics/send-a-message), [Stream file uploads and signed access](https://getstream.io/chat/docs/node/file-uploads/), [OWASP File Upload Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html), [Google Cloud signed URLs](https://docs.cloud.google.com/storage/docs/access-control/signed-urls), and [Azure shared access signatures](https://learn.microsoft.com/en-us/azure/storage/common/storage-sas-overview). These references establish a conservative security boundary, not demand or product-market fit.

## Previous v0.13 engineering status

The application-metadata slice starts from clean synchronized `main` at merged PR #60 (`7dfc43a`) and its successful 19-job exact-main run: **694 Python tests, two skips, 90.01% coverage**, 119 live PostgreSQL tests, 60 SDK tests, package/wheel, container, restore/PITR, load and Kubernetes gates. Official Sendbird, Stream, and Ably contracts expose custom per-message data/metadata, supporting the familiar need to attach application context to chat. Samsarix deliberately narrows that shape to a flat 20-key, 4096-byte JSON-scalar object for named support ticket, classroom assignment, incident severity/runbook, and safe action-reference uses. It is untrusted display/integration data, not authorization, server routing, markup, or executable UI.

HTTP and WebSocket creation accept metadata; an author/admin edit preserves it when omitted, clears it with `{}`, or replaces the full object. SQLite schema 9 and PostgreSQL schema 12 persist default-empty state. History, replies, search, pins, export schema 6, realtime events, and selected signed webhooks inherit the ordinary complete-message contract. Tombstones clear metadata and scrub retained PostgreSQL message events and terminal webhook copies. Unpublished TypeScript SDK 0.8.0 validates writes before transport while keeping inbound metadata optional for released-0.12 compatibility.

Final local verification passes Ruff, mypy, **587 non-PostgreSQL tests with two expected skips and 121 live PostgreSQL tests deselected**, all **61 TypeScript SDK tests**, package inspection, installed-wheel application smoke, and the real TypeScript HTTP/WebSocket recovery journey. The implementation commit (`834fe31`) then passed all 19 jobs in [exact-PR run 33470958333](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33470958333): **708 Python tests with two skips and 90.06% coverage**, all **121 live PostgreSQL tests**, the SDK and installed-wheel metadata smoke, container/Kubernetes acceptance, restore/PITR, and every load profile. Protected merge and exact-main CI remain gates. No registry publication, hosted resource, production deployment, customer data, telemetry, paid service, attachment store, arbitrary nested document store, authorization rule, or executable UI contract is introduced.

Primary references checked on 2026-09-01: [Sendbird message custom data](https://sendbird.com/docs/chat/platform-api/v3/message/message-overview), [Stream message extra data](https://getstream.io/chat/docs/sdk/android/client/extra-data/), [Stream custom-data size contract](https://getstream.io/chat/docs/dotnet-csharp/send-message/), and [Ably message metadata](https://ably.com/docs/chat/api/javascript/message). These references establish contract shape, not demand or product-market fit.

The shared-pins slice starts from clean synchronized `main` at merged PR #59 (`3d124ed`) and its successful 19-job exact-main run: **689 Python tests, two skips, 89.98% coverage**, all live PostgreSQL/load/recovery jobs, 59 SDK tests, package/wheel smoke, container and Kubernetes acceptance. Official Discord documentation now exposes a dedicated pin permission plus paginated channel pins ordered by pin time; Slack records pin time and actor; Stream exposes `pinned_at`, `pinned_by`, a distinct pin permission, and paginated pinned-message UI. Those primary sources support a familiar shared-curation shape for accepted support resolutions, classroom resources, incident runbooks/decisions, and community guidelines. They do not prove demand or justify a full role, notification, attachment, or frontend system.

Samsarix adds a room-scoped `room:pin` capability rather than requiring deployment-wide `admin`. Pin mutation also requires `room:read`, binds signed callers to their token subject, and requires operator/local callers to name the application actor without letting that value replace the authenticated administrative audit actor. Pin/unpin is idempotent; the paginated list is newest-pin-first; real changes commit pin metadata, a metadata-only audit record, optional signed webhook, and `message.pin.updated` realtime state together. SQLite schema 8, PostgreSQL schema 11, and export schema 5 carry nullable pin time/actor metadata. Tombstones clear pins, cancel or scrub outbox payloads, and redact pin actors/content/metadata in retained PostgreSQL events. Unpublished TypeScript SDK 0.7.0 adds list/pin/unpin methods and validates the new event while retaining released-0.12 compatibility through optional fields.

Final local verification passes Ruff lint/format, mypy, **575 non-PostgreSQL tests with two expected skips and 119 live-PostgreSQL tests deselected**, all 60 TypeScript SDK tests, and TypeScript package inspection. A clean sdist/wheel passes Twine; a fresh runtime-only wheel environment passes `pip check`, reports no known third-party vulnerabilities, and completes the installed HTTP/thread/reaction/pin/search/WebSocket/read-state/typing/control/webhook/export/lifecycle/backup smoke. Live PostgreSQL migration/storage/parity/privacy, container and Kubernetes acceptance, exact-PR CI, protected merge, and exact-main CI remain gates for this slice. No registry publication, hosted resource, production deployment, customer data, telemetry, paid service, notification system, attachment store, private bookmark, or host-owned role database is introduced.

Primary references checked on 2026-08-31: [Discord message/channel pin contract](https://docs.discord.com/developers/resources/message), [Slack pin-list metadata](https://api.slack.com/methods/pins.list), [Stream pinned-message model](https://getstream.io/chat/docs/java/pinned-messages/), and [Stream paginated pinned-message list](https://getstream.io/chat/docs/sdk/android/compose/guides/pinned-messages/). These references establish contract shape, not product-market fit.

The bounded-reactions slice starts from clean synchronized `main` at merged PR #58 (`818a305`) and its successful 19-job exact-main run: **684 Python tests, two skips, 90.16% coverage**, all live PostgreSQL/load/recovery jobs, 58 SDK tests, container and Kubernetes acceptance. Current Sendbird and Stream documentation both expose actor-bound reaction keys, grouped counts and realtime updates as a standard low-noise response primitive. Samsarix applies that shape to named support acknowledgement/resolution, classroom feedback and incident coordination without adding an emoji asset service, ranking, polling, or another delivery channel.

Signed writers can idempotently add/remove one validated reaction key; operator/local callers must supply the actor because their shared credential has no end-user subject. Each message is capped at 20 distinct keys. SQLite schema 7 and PostgreSQL schema 10 keep actor-unique rows and sorted grouped summaries in the same transaction. History, replies, search, export schema 4, realtime relay and selected signed webhooks expose the current summary. Tombstoning deletes reaction actors, clears counts and redacts retained PostgreSQL reaction-event actors so the durable privacy boundary follows message deletion; already-delivered or concurrently in-flight copies remain receiver responsibilities. Unpublished TypeScript SDK 0.6.0 adds `addReaction()`/`removeReaction()` and validates `message.reaction.updated`, while keeping the new message field optional for released-0.12 compatibility. PR #59 and its exact protected-main commit passed all local, package, live-PostgreSQL, container, Kubernetes, and 19-job CI gates; no registry publication, hosted resource, telemetry, paid service, attachment system, custom emoji hosting or production deployment was introduced.

Final local verification passes Ruff, formatting, mypy, **571 non-PostgreSQL tests with two expected skips and 118 live-PostgreSQL tests deselected**, all 59 TypeScript SDK tests, package inspection and dependency audit, and the real TypeScript HTTP/WebSocket recovery smoke. A clean sdist/wheel passes Twine; a fresh runtime-only wheel environment passes `pip check` and the installed HTTP/thread/reaction/search/WebSocket/read-state/typing/control/webhook/export/lifecycle/backup smoke. Live PostgreSQL migration/storage/privacy tests, container/Kubernetes acceptance and exact-PR/protected-main runs remain CI merge gates.

Primary references checked on 2026-08-31: [Sendbird reaction add/list/remove and 20-key bound](https://sendbird.com/docs/chat/platform-api/v3/message/reactions-and-emojis/reactions-and-emojis-overview), [Sendbird realtime reaction handling](https://sendbird.com/docs/chat/sdk/v4/javascript/message/managing-a-message/react-to-a-message-in-a-group-channel), and [Stream reaction identity/type model](https://getstream.io/chat/docs/go-golang/send-reaction/). These references establish common contract shape, not demand or product-market fit.

The contextual-threads slice starts from clean synchronized `main` at `70ea1a5`, no open pull requests, and a successful 19-job exact-main run. Current official Sendbird and Stream documentation both expose parent-linked, one-depth or direct-reply threading alongside ordinary room/channel messages. Samsarix now applies that established interaction to its named support, classroom, and incident journeys without adding arbitrary nesting, a separate thread store, or a new delivery stream.

HTTP and WebSocket message creation accept optional `parent_message_id`; only non-deleted top-level messages in the same room can receive new replies. The authorized replies endpoint uses chronological, thread-scoped cursor pagination. Ordinary history and search remain flat and include replies, while exports, webhooks, edits, tombstones, unread counts, idempotency, realtime delivery and PostgreSQL relay reuse the existing message contract. SQLite schema 6 and PostgreSQL schema 9 add an indexed self-reference with `ON DELETE SET NULL`, so physical count/age retention can remove an old parent without unexpectedly cascading newer replies; survivors become top-level messages. Export schema 3 records the parent field. Unpublished TypeScript SDK 0.5.0 adds `listReplies()` and `sendReply()`.

Final local verification passes 35 focused Python thread/API/WebSocket/lifecycle tests, all 58 SDK tests, Ruff, mypy, package inspection/audit, and the complete non-PostgreSQL suite (**568 passed, two SQLite-inapplicable skips, 116 PostgreSQL tests deselected**). Additional room-scope, authorization, WebSocket error-recovery, search, and legacy-0.12 SDK compatibility regressions were added during review; PostgreSQL parent validation now follows retention/message mutation lock order. A clean wheel/sdist passes Twine, a runtime-only wheel install passes `pip check`, and the installed artifact completes HTTP/thread/search/WebSocket/read-state/typing/control/webhook/export/lifecycle/backup smoke. The real TypeScript client also passes reply creation/listing plus reconnect recovery against a loopback server. Live PostgreSQL, container, exact-PR, and protected-main evidence remain merge gates. Upgrade requires a verified backup: older binaries reject schemas 6/9, so rollback restores the matching pre-upgrade database. No package is published and no production resource, telemetry, paid service, attachment system, or arbitrary nested-thread claim is introduced.

Primary references checked on 2026-08-31: [Sendbird one-depth message threading](https://sendbird.com/docs/chat/sdk/v4/javascript/message/using-message-threading/create-a-message-thread) and [Stream threads and replies](https://getstream.io/chat/docs/react/threads/). This is feature-shape evidence, not proof of demand or product-market fit.

The dependency-maintenance slice starts from clean synchronized `main` at merged PR #50 (`403eea9`) and its successful 19-job exact-main run. Six independently opened major-version updates are consolidated for one protected verification cycle after primary-source review and focused compatibility runs: mypy 2.3.1, Twine 7.0.0, TypeScript 7.0.2, and SHA-pinned checkout/setup-node/setup-python v7 actions. TypeScript passes all 56 SDK tests, package inspection, dependency audit and the Python-driven HTTP/WebSocket recovery smoke; mypy checks all 18 source modules; Twine validates a clean wheel and sdist.

The otherwise unrelated setup-node bot run `33455499955` exposed one unexpected 503 in the age-pressure profile. PostgreSQL recorded a cycle between a request that touched a rate-bucket row before waiting for the capacity advisory lock and maintenance that held that advisory lock before deleting expired rows. Bucket misses now finish the optimistic existing-row transaction before taking the capacity lock in a fresh transaction, preserving the row-local fast path for existing buckets while making the miss/maintenance order consistent. A driver-only regression requires no database service and enforces the transaction boundary; it explicitly skips when the optional PostgreSQL extra is absent so SQLite-only collection remains valid. The exact combined-head 19-job suite and all five live load profiles remain merge gates. A separate bot failure was a Docker Hub pull timeout before checkout and is not treated as product evidence. This slice changes no public API, schema, product default, license, production resource, telemetry, package publication or paid service.

The measured-reconnect slice starts from clean synchronized `main` at merged PR #47 (`23852f6`), no open PRs, and successful exact-merge 18-job run `33448968548`: **652 Python tests, two skips, 90.08% coverage; 113 live PostgreSQL cases; 339 SQLite-only tests/55 explicit skips; five accepted recovery/load artifacts from the four existing load profiles plus physical PITR; accepted logical restore; passing Kubernetes and hardened-container journeys**. The previous goal turn was verified progress.

This slice adds an independent fifth load profile for the documented reconnect recovery path. During continued open-arrival writes it pauses one real replica only after the kernel/database-idleness barrier, applies freeze/unfreeze and archive across every room from the healthy replica, observes the healthy half of 32 signed clients close with 4409 and receive at least one archived-room reconnect refusal, reopens the rooms, then resumes the stale process after its live count barrier. Its half must fence with 1012; every client must create a second activated connection, reload paginated authoritative state, converge and release its database lease. Reports retain exact population/path counts, latency windows, resource samples and resume-to-reconnected time. All disposable PostgreSQL GitHub Actions services now use the same reviewed 18.6 manifest-list digest already exercised by Kubernetes acceptance.

This is a three-minute, 32-client, single-host shared-runner measurement. It does not model device sleep, mobile network changes, a remote credential issuer, WAN/TLS ingress, database unavailability or failover, packet blackholes, geographically distributed clients, provider routing, sustained soak, or owner capacity. It changes no runtime API, database schema, package dependency/default, production resource, license, telemetry, customer data, publication, or paid service. Final exact-head results and artifact digests belong in the pull-request acceptance record rather than being predicted here.

The live-Kubernetes-acceptance slice starts from clean synchronized `main` at merged PR #46 (`051086f`), no open PRs, and successful exact-merge 17-job run `33444401286`: **640 Python tests, two skips, 90.06% coverage; 113 live PostgreSQL cases; 327 SQLite-only tests/55 explicit skips; 56 SDK tests; four accepted load/recovery measurements; accepted logical restore and physical PITR; and a passing hardened container journey**. The previous goal turn was verified progress.

This slice closes the repository-level manifest-execution gap with a reusable/manual workflow that creates checksum-pinned kind v0.32.0 and kubectl v1.36.1 clients plus a digest-pinned Kubernetes v1.36.1 node. It builds and imports the exact checkout image, provisions a digest-pinned disposable PostgreSQL 18.6 service, creates a one-run CA and internal-DNS certificate, and mounts the hostname-verifying PostgreSQL URL/CA plus test credentials through Kubernetes Secrets. The checked application manifest must produce two ready Pods and the exact two live database identities. A loopback-only smoke client creates and reads a room across separate Pod forwards, authenticates two signed members on different replicas, and requires the same durable message event on both. The workflow then normally replaces ordinal zero and requires its stable identity, readiness and room data to recover.

The acceptance provider is deliberately test-only and ephemeral. A passing single-node shared-runner run does not establish persistent-volume recovery, database operator behavior, old-primary fencing, routing cutover, provider failover/failback, availability-zone behavior, ingress, NetworkPolicy, certificate rotation, immutable published-image provenance, owner capacity, or production availability. Those remain P1 along with broader controlled-host load/soak and kernel-level failure evidence. No runtime API, database schema, default, dependency range, license, production resource, package publication, telemetry, customer data, or paid service changes in this slice. No local Docker, kubectl, kind, or PostgreSQL executable is available; local acceptance is structural/unit/static, and the real cluster remains a final-head CI gate.

The stable-replica-identity slice starts from clean synchronized `main` at merged PR #45 (`258c771`), no open PRs, and successful exact-merge 17-job run `33438571407`: **632 Python tests, two skips, 90.06% coverage; 112 live PostgreSQL cases; 320 SQLite-only tests/55 explicit skips; 56 SDK tests; four accepted schema-v2 load/recovery measurements; accepted logical restore and accepted physical PITR**. The previous goal turn was verified progress.

This slice addresses the unchecked deployment-identity gate. A guarded Kubernetes manifest uses a two-replica StatefulSet and derives `SAMSARIX_CHAT_POSTGRES_INSTANCE_ID` from each stable Pod name through the Downward API. It mounts the PostgreSQL URL and authentication material as files, runs UID/GID 10001 with a read-only root filesystem, drops capabilities and service-account credentials, defines startup/readiness/liveness probes and resource bounds, prefers node separation, exposes only internal Services, and retains one replica during voluntary disruptions. The container image now includes both asymmetric-auth and PostgreSQL extras; the supported Compose profile remains unchanged SQLite-only behavior.

A PyYAML-based repository verifier checks the exact controller/service/selectors, stable identity source, manual `OnDelete` update gate, absence of inline credentials, projected secret paths, non-root/seccomp/read-only controls, probes, resources and disruption budget. Mutation tests prove the verifier fails closed. A real Uvicorn-process PostgreSQL case must reject a second live process using the same ID without logging the operator key or URL password, keep the original owner ready/writable, then permit graceful same-ID replacement with retained state. This proves one configuration error and clean replacement path, not Kubernetes execution, node-loss fencing, NetworkPolicy, provider failover, live version-upgrade safety, public ingress, or production availability. Those gates remain P1.

The physical-recovery slice starts from clean synchronized `main` at merged PR #44 (`b2d2f52`), no open PRs, and successful 16-job post-merge run `33434924691`: **617 Python tests, two skips, 90.08% coverage; 112 live PostgreSQL cases; 305 SQLite-only tests/55 explicit skips; 56 SDK tests; four accepted schema-v2 32-subscriber measurements; and one accepted logical restore**. The previous turn was verified progress.

This slice addresses the next P1 operational gate with a separate reusable/manual PostgreSQL 18.6 physical-PITR workflow. A narrowly scoped probe records schema-8 application state before a plain `pg_basebackup`, after that backup but before a named restore point, and after the target. CI requires a SHA-256 manifest plus `pg_verifybackup`, forces and observes archival of the WAL containing the divergent later state, removes the application role's `CONNECT` privilege from the old primary, then replays into a second cluster on the same disposable host. Acceptance requires the same cluster system identifier, a newer promoted timeline, exact inclusion of base/WAL-target state, exclusion of the later message, search/read-state/audit invariants, five post-recovery mutations, and a still-effective old-primary application-role fence. A content-free schema-1 artifact records the revision, PostgreSQL version, manifest and WAL-inventory hashes, archived-file count, target LSN, timelines, fencing, promotion, and application results.

The probe rejects remote hosts, wrong ports or database names, URL routing parameters, libpq routing overrides, and missing confirmation. The workflow refuses pre-existing scratch paths and creates no production resource. Its base backup and WAL archive share one ephemeral CI host; revoking database access is not storage durability, external STONITH, routing cutover, provider promotion, failback, or measured RPO/RTO. Those deployment gates, controlled-host capacity and owner acceptance remain P1. No production database, deployment, package publication, paid service, runtime implementation/dependency, schema version, product default, license, telemetry, or customer data changes in this slice.

The prior P1 operational slice started from clean synchronized `main` at merged PR #43 (`36408d5`), no open PRs, and successful 15-job post-merge run `33430058562`: **605 Python tests, two skips, 90.06% coverage; 112 live PostgreSQL cases; 293 SQLite-only tests/55 explicit skips; 56 SDK tests; four accepted schema-v2 32-subscriber measurements**. Larger evidence remains explicit rather than promoted: the 15-minute run dropped one schedule slot, the 64-subscriber count case dropped one concurrency slot amid high shared-runner I/O pressure, and the corrected 128-subscriber case dropped two schedule slots around recovery.

This slice addresses the next documented release gate: PostgreSQL-native recovery evidence. A reusable/manual CI workflow provisions PostgreSQL 18.6, initializes schema 8 through the real application, seeds edited and tombstoned history, search, member read state, archived/frozen lifecycle, audit records, realtime state and sequences, then stops the source application. Native `pg_dump` creates a custom-format, role-neutral whole-database archive; `pg_restore` inspects it and restores with `--exit-on-error --single-transaction` into a fresh database created from `template0`. A second application instance must become ready, reproduce the authoritative state, keep message content out of audit data, and complete create/edit/delete plus unarchive/rearchive writes.

The probe requires an exact confirmation value and refuses non-loopback hosts or any database except `samsarix_backup_source`/`samsarix_backup_restore`; it never resets an arbitrary target or echoes HTTP bodies on failure. The [recovery contract](POSTGRES_BACKUP.md) uses current PostgreSQL 18 primary documentation to separate this portable logical restore/rollback evidence from physical base backups, continuous WAL archiving, PITR, split-brain fencing and provider failover. Those physical gates, controlled-host capacity and owner deployment acceptance remain P1. The archive contains plaintext product data; operators own protected credentials, roles/grants, encryption, off-host retention, deletion propagation, webhook deduplication and measured RPO/RTO. No production database, deployment, package publication, paid service, runtime dependency, schema version, product default or license changes in this slice.

The next measurement slice starts from clean, synchronized `main` at `5823f2d`, no open PRs and successful 15-job post-merge run `33425965124`. Manually dispatched 128-subscriber count-fault run `33426806353` failed before traffic: all 64 sockets on replica 1 closed with 1012, leaving no ready clients or offered cycles. This is failed evidence, not a capacity result. The first report lacked a reason. Version 2 diagnostics add time-windowed latency, procfs application/driver CPU and I/O, host pressure, PostgreSQL state/wait categories, cumulative database counters and counts of four static warning labels from a bounded tail. Identical repeat `33427630829` then failed during admission with count-limit fence signals on **both** replicas, proving that the harness's deliberately reduced bound—not a production default—was below its own 128-client presence burst. The count-fault threshold now scales to at least two presence events per configured subscriber; 32-client behavior remains 100, while the 128-client profile uses 256 and still must naturally exceed it during the pause. Product defaults and runtime code are unchanged. Reports exclude queries, raw logs, identities and network/database secrets. Unit tests validate parsing, null/empty semantics, content isolation, exact time buckets and threshold scaling before repeating the same case again.

Corrected run `33427933497` admits all 128 clients, naturally reaches backlog 259 against the 256-event injected bound, fences the paused replica once, reconnects 80 affected clients in 1.771 seconds, converges all 128 clients, verifies all 128 authorization denials and drains every lease. It remains a **failed measurement**: two of 3600 open-arrival slots were over 50 ms late around the synchronized recovery window. No missing or duplicate version is hidden on uninterrupted streams. Rather than loosen that contract, version 2 now records bounded per-drop index/reason/phase/relative time/lateness, and a 64-client midpoint will test the next supportable reconnect population. This is evidence of a current offered-rate/reconnect capacity boundary, not a product-default or correctness failure.

The first 15-minute steady run, `33426800102` at merged `5823f2d`, is also retained as failed evidence: one of 18000 scheduled cycles was late. The other 17999 creates, 3600 edits and 1800 deletes were accepted and persisted; all 32 clients converged with no missing/duplicate acknowledged versions, all authorization checks passed, every lease drained, HTTP POST p95/p99 was 11.30/12.78 ms and both replicas' live-delivery p99 was about 65.1–65.2 ms. Application-process PSS peaks were 64.6/66.7 MiB and the scratch database grew from 8.66 to 36.67 MB; driver/PostgreSQL memory was not included in this schema-v1 run. A single unlocated dropped slot prevents acceptance and is not converted into a successful percentile. Report SHA-256: `B62DF46F0E0EC7A9490EA65156F9980BB7E0B4F47A11EA318E4D26BA9C37F1D5` (artifact `postgres-load-steady-33426800102-1`, 30-day retention).

The 64-client midpoint in run `33428703695` also fails strict acceptance by one dropped-concurrency slot: its 32-operation driver bound was full at synthetic cycle 1260, 64.941 seconds into the recovered phase. It otherwise naturally crosses the scaled 128-event count limit, fences 32 paused-replica sockets, reconnects 40 affected clients in 0.950 seconds, converges all 64 clients without healthy-stream loss and drains all leases. The drop coincides with increasing host-wide I/O PSI (some avg10 up to 39.71%), sampled WAL synchronization/write waits plus advisory/transaction/tuple locks, HTTP POST p99 1139.64 ms and live-delivery p99 1383.72/1496.68 ms; host CPU PSI avg10 peaks at 1.18% and memory PSI remains zero. These instantaneous/host-wide observations establish correlation, not which workload owns the pressure or a root cause. The result supports fast durable storage, controlled-host measurement and explicit write-capacity sizing; it does not justify weakening durability or increasing client-side concurrency to manufacture a pass. Report SHA-256: `D1F8F50BAA55FA80194AE84AB0372EA5FE01EEBD691C450017B3BE2E8A87B5E5` (artifact `postgres-load-count-33428703695-1`, 30-day retention).

PR #41 merged as `79d3afd` after all 14 final-head jobs passed on PostgreSQL 18.6: **591 Python tests, two skips, 90.06% coverage, 112 PostgreSQL cases and 56 SDK tests**, plus four real-process 180-second load/recovery profiles. Final-head metrics, artifact hashes and the rebuilt runtime-only installed-wheel journey are recorded on [PR #41](https://github.com/Deathcharge/samsarix-chat-engine/pull/41#issuecomment-5482649644). Post-merge run `33424832229` is a separate acceptance record; it is not inferred green from the PR.

Follow-up installation review found a regression in the newly added test module: importing its PostgreSQL harness unconditionally broke collection when only the `test` extra was installed. A clean runtime/test-only environment at `79d3afd` reproduced `ModuleNotFoundError: psycopg` before any cases ran. The module now uses the same explicit optional-driver skip as existing PostgreSQL tests; all 64 harness cases still execute when the driver is present. A new isolated SQLite-only CI job installs just `.[test]`, asserts the PostgreSQL driver is absent, checks dependencies and runs pytest. Local test-only verification passes **281 tests with 55 PostgreSQL/inapplicable skips**; the full-driver harness still passes all 64 cases. This fixes collection rather than requiring PostgreSQL for the supported SQLite product. No runtime, schema, dependency range, license or deployment change is involved.

P1 priorities remain longer/larger **controlled-host** load/soak and latency diagnosis, kernel packet loss/database failover, PostgreSQL-native backup/PITR/restore/application rollback, and owner deployment acceptance. The final PR #41 steady run's POST p99 was 636.56 ms versus 13.15 ms in the earlier corrected run; no cause is established by different shared runners. Both measurements remain visible, and correctness acceptance is not a latency guarantee. P2 telemetry/cache remains demand-led; publication, live upgrades and legal decisions remain owner-controlled.

### PR #41 evidence (historical)

Revalidated baseline: clean `main...origin/main` at `a019399`, no open PRs, and all ten [post-merge checks](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33419123216) passing: 527 Python tests, two skips, 90.09% coverage, 112 PostgreSQL cases and 56 SDK tests. The previous goal turn made verified progress through PRs #39/#40. The next P1 is measured load/recovery evidence: existing bounded process cases do not establish offered-rate behavior, client-state convergence under sustained traffic, resource growth or measured reconnect populations.

A checkout-only [load/recovery harness](POSTGRES_LOAD.md) schedules bounded open-arrival create cycles against two real CLI processes, with signed multi-room subscribers, dependent edits/deletion, state/authorization checks and optional count/age/natural-gap fault injection while traffic continues. It counts missed arrivals rather than reducing offered load, separates HTTP/live/history measurements, records sampling scope and configuration, and refuses remote/non-scratch/shared database targets or overwriting reports. Four disposable CI profiles preserve JSON evidence even on failure; a manual longer-run workflow supports bounded soak probes. There are 64 passing local accounting/safety tests. No runtime, schema or dependency change was needed for this measurement increment.

The first live run `33422510240` failed all four new profiles because the harness incorrectly required original content in older events that deletion intentionally scrubs. That failure is preserved, not classified as accepted capacity evidence. The corrected [run 33423357906](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33423357906) passes all four measured profiles: each starts all 3600 offered cycles over 180 seconds at 20/s, all 32 clients converge, and unaffected live streams cover every acknowledged event exactly once. Fault profiles account for deliberate lifecycle refusals and history-based recovery; no dropped cycles, unknown HTTP outcomes or unexpected refusals are hidden. Observed resume-to-reconnected time is 0.537–0.861 seconds, excluding intentional pause time. Exact environment, counters, scoped percentiles, memory samples, failed-run explanation and four artifact digests are in the guide. These are shared-runner observations, not production limits or a three-minute leak proof.

Review additionally requires event-type/version consistency, blocks libpq environment redirection and WebSocket proxies, and tests history/redaction overlap without resurrecting content. The reviewed local suite passes **479 tests, two inapplicable skips, 112 deselected** (`dist/pr41-reviewed-local.xml`); all 64 harness cases, Ruff lint/format and mypy pass. Final reviewed-head/full-matrix, fresh package and post-merge verification remain required and will be recorded on PR #41. Longer/larger populations, kernel packet loss, database failover, native backup/PITR and owner deployment acceptance remain P1.

The research rationale is explicit in the load guide: open arrivals avoid response-dependent omission, dropped work must remain visible, PostgreSQL snapshot behavior matters for observers, and process memory samples cannot establish whole-deployment peaks. Supported SQLite alpha, unreleased PostgreSQL preview, schemas 5/8, Samsarix LLC/MPL-2.0 attribution, dependencies, privacy defaults and hosted infrastructure remain unchanged. Remaining P1 includes broader/longer measured profiles, kernel network/database failover, native backup/PITR/restore/rollback and deployment acceptance; optional telemetry/cache remains demand-led P2. Production operation, package publication and formal legal decisions remain owner-controlled.

Initial local `python -m pytest -q -m "not postgres" --timeout=60 --tb=line --junitxml=dist/pr41-initial-local.xml` passes **469 tests, two inapplicable skips, 112 deselected**. Ruff lint/format (74 files), mypy (18 core source files) and the harness's help command pass. Two initial fake-clock test expectations exposed the need to model event-loop task scheduling before advancing simulated time; corrected clock fixtures and actual-start lateness accounting now pass. No live performance or production capacity claim is inferred from these unit/local results.

PR #41 final dependency review additionally updates all three PostgreSQL CI service declarations from 18.4 to 18.6, whose official release includes security fixes. This changes only disposable CI infrastructure, not runtime dependencies, schema or any deployed database. The load guide preserves versioned historical measurements and links the upstream operator migration notes. Fresh final-head acceptance on 18.6 is required before merge.

### PR #40 evidence (historical)

PR #39 merged as `8a03286` after all ten final-head checks passed: 526 Python tests, two skips, 90.08% coverage, 112 PostgreSQL cases and 56 SDK tests. Its fresh runtime-only wheel smoke and artifact hashes are recorded on the PR. However, [post-merge run 33416686370](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33416686370) passed nine jobs and failed Linux Python 3.14 in the pre-existing blocked-DNS deadline test. The new process-recovery cases passed. This failed run is not accepted as green or hidden behind an unexplained retry.

The failing test required the complete dispatcher iteration, including its SQLite outcome write, to finish within 0.8 seconds. The network deadline deliberately excludes storage. The CI task was awaiting a thread-backed future after DNS entry, consistent with outcome persistence; the original case passed locally. Test-only commit `95dd4c4` independently reproduces the invalid assertion by delaying only outcome recording by one second: the correct network transport still produces the same failure. Deadline tests now observe the real transport's exit separately, retaining the non-cancelling 0.8-second assertion, then allow a separate bounded three-second storage wait. No production timer or transport implementation is replaced. The delayed-storage case still requires one persisted timeout, no extra DNS work and no connection from a late resolver result. Slow-header, late-success and TLS-stall checks use the same explicit phase boundary.

An initial local targeted run also exposed a separate slow-header setup failure: the 0.15-second configured budget could expire before the receiver saw a request. That HTTP fixture now uses a 0.5-second network budget, still shorter than the 0.8-second completion assertion, while the peer continuously sends headers every 0.03 seconds. Physical peer closure is checked before awaiting storage. This tests a total deadline without assuming 150-ms thread/socket startup on every Windows run. Cancellation/stop assertions and the real blocked-DNS shutdown test remain intact. Final targeted/full tests, package checks and CI evidence for this follow-up are recorded on its PR before merge.

This follow-up changes acceptance tests and documentation only. The embeddable signed private/support-room journey, single-instance SQLite alpha, unreleased PostgreSQL preview, schemas 5/8, dependencies, Samsarix LLC/MPL-2.0 attribution and operating costs are unchanged. Remaining P1: measured load/soak, wider outage/reconnect stress, kernel network/database failover, native PostgreSQL backup/PITR/restore/application rollback and deployment acceptance. Optional telemetry/cache remains demand-led P2. Publication, production infrastructure and formal legal decisions remain owner-controlled; no new telemetry, credentials or spending.

Corrected local acceptance: `python -m pytest -q tests/test_webhook_deadlines.py tests/test_webhook_transport.py --tb=line --timeout=30` passes **34 tests**; `python -m pytest -q -m "not postgres" --timeout=60 --tb=line --junitxml=dist/pr40-local.xml` passes **415 tests, two inapplicable skips, 112 deselected**. Ruff lint/format, mypy and diff checks pass. An intermediate focused run timed out while reporting a CLI shutdown fixture failure; that unchanged CLI case then passed individually and in the corrected focused/full runs. All failures are retained as evidence rather than removed assertions. Live PostgreSQL still requires CI because no local PostgreSQL/Docker executable is available. Final-head CI, build/installed-wheel acceptance and post-merge results remain separate checks, recorded on the follow-up PR.

Final review replaces the DNS test's fixed 50-ms sleep with a bounded observation that the transport's active native function has actually exited. A resolver's own `finally` notification happens before its caller can inspect the returned address, so that notification alone cannot prove absence of a later connection. The test now waits for real worker cleanup before checking the connection spy and before restoring its patches, including cancellation/stop cleanup. This strengthens the no-late-send evidence without changing runtime code.

### PR #39 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `67fa74b` (merged [PR #38](https://github.com/Deathcharge/samsarix-chat-engine/pull/38)), no open PRs before this slice, and all ten [post-merge jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33413523393) passing: **523 Python tests, two SQLite-inapplicable skips, 90.09% branch-inclusive coverage**, including 109 PostgreSQL tests and 56 SDK tests. Final packages, runtime-only installed-wheel acceptance and artifact digests are recorded on PR #38. The previous goal turn made verified implementation progress. Revalidated local baseline: `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **414 tests, two skips, 109 deselected**.

The next gap is evidence scope: current live-lag/retained-gap tests inspect foundations, relay objects and controlled application objects, but do not establish the outcome when an entire operating-system process is paused. Three new Linux acceptance cases start two real CLI/Uvicorn replicas with matching settings, signed room-scoped members and an independent read-only database observer. The test stops only its own child, confirms kernel-stopped state and then verifies all of that child's database sessions are idle outside transactions; otherwise it resumes/retries. This avoids freezing an unrelated global database lock. Cleanup always resumes the child before bounded process teardown. The barrier follows Linux's documented [process state](https://man7.org/linux/man-pages/man5/proc_pid_status.5.html), Python's [child signal API](https://docs.python.org/3/library/subprocess.html#subprocess.Popen.send_signal), and PostgreSQL's [activity view](https://www.postgresql.org/docs/current/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW).

Count/age cases preserve a live 120-second lease while the stopped replica accumulates a real backlog. The retained-gap case uses natural three-second expiry and waits for the healthy process's actual periodic pruning; it does not rewrite a clock, lease, cursor, event row, application method or prune timer. During the pause, the healthy replica accepts creates/edits/deletion, freeze/unfreeze and archive/reopen, while an unrelated-room socket proves continuity. After resume, stale sockets must close 1012 without obsolete frames; generation/cursor recovery, authoritative edited/tombstoned history, a four-member reconnect burst, room authorization and fresh cross-replica delivery are checked. Client keepalive timers are disabled so they cannot impersonate server-side fencing. This is bounded fault acceptance, not a throughput benchmark, reconnect-storm capacity claim or kernel packet-loss/database-failover test.

The new cases are Linux-only because their pause barrier requires SIGSTOP and procfs. They run in the existing live PostgreSQL CI jobs against the guarded test database. No PostgreSQL/Docker executable is available locally. Local collection/lint/type checks and full live CI remain acceptance gates; no runtime fix is assumed before evidence. Supported single-instance SQLite alpha, unreleased PostgreSQL preview, schema versions 5/8, dependencies, MPL-2.0 attribution, telemetry and operating infrastructure are unchanged. Measured load/soak, broader outage/reconnect stress, native PostgreSQL backup/PITR/restore/rollback and deployment acceptance remain P1; optional telemetry/caching remains demand-led P2. Publication, formal legal decisions and production setup remain owner-controlled.

Initial [PR #39 run 33414942695](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33414942695) at `cb612e7` passes **all ten jobs, 526 Python tests, two skips and 90.11% branch-inclusive coverage**, including **112 PostgreSQL tests** and the three new cases in both database jobs. No runtime defect was observed or patched. Local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **414 tests, two inapplicable skips, 112 deselected**; the three new cases explicitly skip on Windows. Ruff lint/format, mypy, diff checks and collection pass. Review strengthens exact frame/room-isolation assertions, waits for terminal generation/cursor and physical-cleanup conditions rather than assuming instantaneous cleanup, and cancels/awaits every concurrent handshake before closing its context stack. Final-head CI and rebuilt artifact evidence are required and recorded on PR #39 before merge; initial results do not stand in for those final checks.

### PR #38 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `993c19e` (merged [PR #37](https://github.com/Deathcharge/samsarix-chat-engine/pull/37)), no open PRs before this slice, and all ten [post-merge jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33408558081) passing: **490 Python tests, two SQLite-inapplicable skips, 89.50% branch-inclusive coverage**, including 109 PostgreSQL tests and 56 SDK tests. Fresh runtime-only installed-wheel smoke and artifact digests are recorded on PR #37. The previous goal turn made verified implementation progress.

This slice addresses the observed P1 webhook transport/shutdown gap. Test-only commit `c11ea61` preserves six failing regressions: slow headers exceeded the budget; blocked DNS held delivery/shutdown open; cancellation permitted additional unresolved work; a late success was accepted; and a stopped dispatcher still claimed a row. The new internal transport owns one monotonic attempt budget, one cancel-owned socket and one daemon worker/slot per dispatcher. Numeric IPv4/IPv6 connections avoid a second DNS lookup. TLS retains the original hostname/SNI and platform certificate verification. Response reads use the remaining total budget rather than resetting an idle timeout. Completed jobs release their delivery/signing closures while idle.

Timeout records a retryable sanitized failure; cancellation/stop leaves an unfinished claim unrecorded for existing restart/lease recovery. Native DNS cannot be killed, so a lingering resolver occupies the sole slot and suppresses new claims until it returns. Its expired result cannot connect or replace a recorded outcome, and its daemon ownership does not delay interpreter exit. This is an explicit resource/liveness tradeoff, not a hard-real-time or exactly-once claim. One claimed body/secret closure can remain in that worker until return/process exit. No extra network dependency, hosted service, telemetry or paid resource was introduced. The design uses Python's documented [TLS verification](https://docs.python.org/3/library/ssl.html#ssl.create_default_context), [socket shutdown](https://docs.python.org/3/library/socket.html#socket.socket.shutdown) and [daemon lifetime](https://docs.python.org/3/library/threading.html#thread-objects) behavior.

Verification includes real HTTP/TLS slow-header peers and physical disconnects, stalled TLS handshake, trusted and rejected certificates, original Host/SNI, DNS pinning, late outcomes, cancellation/stop, invalid budgets, worker capacity/reuse/start failure, closed-loop completion, released idle references, and a real Uvicorn subprocess exiting cleanly while its resolver remains blocked. Shared bounded child-process helpers were extracted without changing PostgreSQL process-test semantics. Full local tests, live PostgreSQL/platform CI, fresh artifacts and merge evidence remain gates until recorded below/on the PR.

Local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **412 tests, two inapplicable skips, 109 deselected**. Ruff lint/format and mypy pass. Live PostgreSQL and container acceptance cannot run locally because no PostgreSQL/Docker executable is available; both remain CI gates, alongside the supported Python/OS matrix, SDK and installed-wheel journeys.

Initial [PR #38 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33411405461) found Python 3.10's distinct `asyncio.TimeoutError` and a Python 3.11 TLS-timeout case reporting SQLite `file is not a database`. Review identified an unsafe descriptor-ownership window: cancellation closed a socket while native I/O could still own its descriptor. Test-only commit `2901321` deterministically reproduces that premature release. Cancellation now performs base-socket shutdown without changing OpenSSL state or releasing the descriptor; only the transport owner closes after native I/O unwinds. HTTP protocol cleanup also defers descriptor release to that owner. Async timeout errors are normalized across supported Python versions. The database symptom is consistent with descriptor reuse; the deterministic regression verifies the ownership flaw rather than claiming a deterministic reproduction of that exact corruption. The initial draft is not accepted for merge; corrected full-matrix and package verification remain required.

Corrected local checks pass **44 focused tests on Windows CPython 3.14.7 and 3.11.9**. The full non-PostgreSQL suite passes **413 tests, two skips, 109 deselected** at the owner-close correction; the final transport uses `asyncio.wait()` and one builtin timeout contract to avoid version-specific `wait_for()` cancellation races. Ruff lint/format, mypy and diff checks pass. Final-head CI and rebuilt installed-wheel evidence will be recorded on PR #38 before merge.

Corrected implementation [run 33412072064](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33412072064) at `81d728f` passes **all ten jobs, 522 Python tests, two skips and 90.06% branch-inclusive coverage**, including all **109 PostgreSQL tests**, the Python 3.10–3.14/Linux and 3.12/Windows matrix, SDK installed-consumer recovery journey, container smoke and dependency audit. Final local non-PostgreSQL verification again passes **413 tests, two skips, 109 deselected**. Five repeated real-TLS runs on Python 3.11 each pass all seven cases. Rebuilt wheel/sdist pass Twine; a runtime-only installed wheel passes `pip check` and the real HTTP/WebSocket/search/read-state/typing/moderation/webhook/export/lifecycle/backup journey. Final documentation-head CI and artifact digests are recorded on PR #38 before merge; post-merge CI remains required. This is verified reliability progress, not promotion of the PostgreSQL preview to production support.

Remaining P1 work, ordered: combined lifecycle/outage/reconnect and separate-process live-lag/retained-gap recovery; measured load/soak; PostgreSQL-native backup/PITR/restore/application rollback and deployment acceptance. P2 telemetry/caching remains demand-led. Schema versions remain PostgreSQL 8 / SQLite 5, with no dependency or license change. The product remains an embeddable private/support-room engine: supported single-instance SQLite alpha plus unreleased PostgreSQL preview. Publication, formal legal decisions and production deployment remain owner-controlled; this increment does not clear those gates.

Final ownership review found a completion-before-wait race: a fast worker could finish/close its budget before the caller queried that budget to begin waiting, incorrectly reporting timeout instead of its completed result. Test-only commit `a4acbc5` preserves a deterministic barrier regression failing with `TimeoutError`. The caller now uses the remaining absolute deadline and the completion future, without confusing owner cleanup with expiration. The final local suite passes **414 tests, two skips, 109 deselected**, and the focused Python 3.11 suite passes **45 tests**; lint/format/mypy/diff checks pass. This adds one test beyond the 522-test run above; final PR evidence identifies the accepted commit, complete CI count and rebuilt artifacts. The earlier documentation-only CI run is superseded by this correction.

### PR #37 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `7408214` (merged PR #36), no open PRs before this slice, and all ten [post-merge jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33406135740) passing: **488 Python tests, two SQLite-inapplicable skips, 89.54% branch-inclusive coverage**, including 107 PostgreSQL tests and 56 SDK tests. Final-head CI, fresh package installation and artifact digests are recorded on PR #36. The previous goal turn made verified implementation progress.

PR #37 targets the next evidence gap: webhook claim concurrency and recovery previously used two storage objects and manually expired a database row. New acceptance starts two independent CLI/Uvicorn processes using the same destination, event selection and signing settings. A bounded loopback receiver independently verifies the raw-body HMAC, timestamp and delivery ID, then withholds each HTTP acknowledgement until the test releases it. While one worker is blocked in its leased delivery, the other proves progress on another message and live WebSocket fan-out; database rows show different live claim owners. The first child is killed, its connection closes without acknowledgement, and its claim remains stranded until the real default 60-second lease expires. No database clock, expiry or application method is rewritten.

Two cases then check either identical signed payload/ID recovery by the survivor with a fresh timestamp, or deletion of the message before reclaim, cancellation of the queued body and denial of replay. A later acknowledged message proves survivor progress after the original lease's natural expiry. The crash-before-outcome case deliberately shows two receiver requests but only one recorded attempt: counters describe persisted outcomes, not all network attempts or downstream effects. Receivers still need durable deduplication. The design follows PostgreSQL's [queue-oriented row locking](https://www.postgresql.org/docs/current/sql-select.html#SQL-FOR-UPDATE-SHARE) and the [Standard Webhooks specification](https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md).

Local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **381 tests, two inapplicable skips, 109 deselected**. Ruff lint/format, mypy, diff checks and collection of the two new cases pass. The cases have a 150-second per-test ceiling to allow real 60-second expiry plus bounded startup/teardown; they run in the existing PostgreSQL CI jobs, not against a production database. No PostgreSQL/Docker executable is available locally. Live CI, final package verification and merge remain gates, with exact results and digests to be recorded on PR #37.

Initial implementation [run 33407103518](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33407103518) at `64cf953` passes **all ten jobs, 490 Python tests, two skips, 89.54% coverage**, including **109 PostgreSQL tests** and 56 SDK tests. Both new natural-expiry cases pass in the two database jobs; CI also builds, validates and smoke-tests the installed wheel. Review strengthened the live control-claim assertions and corrected the operational timeout claims below; final-head CI and rebuilt artifact verification remain required before merge.

Review found a separate **P1 transport/shutdown deadline gap**: `http.client` applies the configured timeout to blocking socket operations, not the complete request. An actual `_send_request` loopback probe with a 0.3-second setting and six response headers sent 0.1 seconds apart returned 204 after **0.614 seconds**; the receiver exited cleanly. The operations runbook's claimed shutdown bound was therefore incorrect. Documentation now describes the existing socket timeout and need for a supervisor-enforced termination deadline. A total delivery/shutdown deadline, slow-header/DNS/TLS tests, and bounded treatment of outstanding thread work are the next local hardening priority; the worker-crash tests do not solve that gap or prove no overlapping external requests after expiry. Python's [HTTP client documentation](https://docs.python.org/3/library/http.client.html#http.client.HTTPConnection) distinguishes blocking-operation timeouts from an overall deadline.

Remaining P1 work, ordered: total webhook delivery/shutdown deadline; combined lifecycle/outage/reconnect and separate-process live-lag/retained-gap recovery; measured load/soak; PostgreSQL-native backup/PITR/restore/application rollback and deployment acceptance. P2 telemetry/caching remains demand-led. No schema (PostgreSQL 8 / SQLite 5), dependency, runtime implementation, license, telemetry, paid infrastructure or publication change in this slice. The private/support-room product stays a supported single-instance SQLite alpha and an unreleased PostgreSQL preview; formal legal decisions, publication and production setup remain owner-controlled.

### PR #36 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `dcf28bf` (merged PR #35), no open PRs before this slice, and all ten [post-merge jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33403671485) passing: **480 Python tests, two SQLite-inapplicable skips, 89.50% branch-inclusive coverage**, including 99 PostgreSQL tests and 56 SDK tests. SDK 0.4.0 also passed an installed-tarball consumer's typecheck and real-server recovery journey; Python wheel/sdist validation and runtime-only installed-wheel smoke passed. Exact final artifacts and digests are recorded on PR #35. The previous goal turn made verified implementation progress.

PR #36 addresses an evidence gap rather than assuming a new runtime bug: storage concurrency tests and two application objects did not prove shared limits or mutation fan-out across separate operating-system processes. New acceptance uses two real CLI/Uvicorn children with distinct database application names and signed members. A separate connection holds the relevant row/advisory lock until `pg_stat_activity` reports both replicas waiting; the observer uses autocommit to avoid cached transaction snapshots. These semantics follow PostgreSQL's [activity monitoring](https://www.postgresql.org/docs/current/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW) and [locking documentation](https://www.postgresql.org/docs/current/explicit-locking.html).

The bounded cases cover concurrent identical creates (one 201/one 200 and one message), two ordered author edits, duplicate deletes (two 204 responses but one delete event), non-author rejection on either replica, tombstone/history/reconnect convergence, global/per-room socket admission with release/reuse, a deployment-wide room cap, and shared-subject HTTP/search/WebSocket/typing rate budgets across replicas and rooms. Message frames are retained for exact normal-operation prefix comparisons through a later committed sentinel; pre-delete frames are read before deletion intentionally scrubs retained payloads. The rate tests use database time, leave at least 30 seconds in the fixed minute and assert that the contention burst stayed within that window. Distinct subjects and transport/scope budgets remain independent by contract.

Initial [CI run 33405038257](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33405038257) at `8ae66b3` passed **104 PostgreSQL tests** but caught one new fixture mistake: room listing returns a plain list, not a message-page `items` envelope. The test now checks the real response shape without changing any capacity assertion. Two additional WebSocket/typing rate cases were added after that run. Local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **381 tests, two inapplicable skips, 105 deselected** at the initial six-case head. Ruff lint/format, mypy and diff checks pass. No PostgreSQL or Docker executable is available locally; live CI, final packaging and merge evidence remain gates and will be recorded on PR #36.

Remaining P1 work, ordered: killed webhook-worker claim recovery; combined lifecycle/outage/reconnect storms and separate-process live-lag/retained-gap recovery; measured load/soak; PostgreSQL-native backup/PITR/restore/application rollback and deployment acceptance. P2 telemetry/caching remains behind operator demand. These are small bounded contention scenarios, not benchmarks, fairness guarantees or exactly-once delivery under faults. No production schema, dependency, license, telemetry, hosted infrastructure or operating-cost change. The product remains a supported single-instance SQLite alpha with an unreleased PostgreSQL preview for embedding private/support rooms; publication and production operation remain owner-controlled.

Corrected implementation [run 33405292958](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33405292958) at `46f6f32` passes **all ten jobs, 488 Python tests, two skips, 89.50% coverage**, including all **107 PostgreSQL tests** and **56 SDK tests**. The eight new process cases pass in both database jobs. Final local non-PostgreSQL verification passes **381 tests, two skips, 107 deselected**. A fresh runtime-only wheel install passes `pip check` and the real HTTP/WebSocket/search/read-state/typing/moderation/webhook/export/lifecycle/backup smoke; wheel/sdist build and Twine checks pass. Final-head CI and rebuilt artifact digests are recorded on PR #36 before merge.

Documentation review also removed a misleading mandatory listener gate: code emits transactional `pg_notify` but the relay has no `LISTEN` consumer. Polling is the implemented correctness path and remains covered by process reset/stall recovery. Notification-assisted latency is optional and will need listener-specific fault tests only if introduced; no unimplemented listener is presented as tested, and measured load/failover gates remain open.

### PR #35 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `fe381bc` (merged PR #34), no open PRs and all ten [post-merge jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33400724290) passing: **480 Python tests, two SQLite-inapplicable skips, 89.49% branch-inclusive coverage**, including 99 PostgreSQL tests. Its final-head run `33400296315` passed 480 tests at 89.52%; rebuilt artifacts and installed-wheel journeys passed, with digests recorded on PR #34. The previous goal turn made verified implementation progress.

The next observed recovery risk was client-side. SDK baseline `npm test` passed 18 tests, but nine new regressions failed: `connect()` resolved on `ready` before history; ready/history/short activated connections reset the retry budget; stalled credentials/transport/history/activation never timed out; and protocol cleanup passed wire code 1002 to a browser API that rejects it. Test-only commit `c64239e` preserves that evidence. A strengthened cancellation check also found a timer could be scheduled after a reconnect-state listener closed the session.

Unpublished SDK 0.4.0 now waits for ready/history and its post-history ping reply before resolving `connect()` or allowing sends. One configurable 10000-ms default deadline covers each attempt, including credential lookup. A separate 10000-ms default stability period after activation resets the automatic retry budget; flapping connections do not. Both durations are bounded positive integers. Attempt generations, detached transport callbacks and timer ownership fence late results, repeated error/close callbacks, listener-driven cancellation and nested reconnects. Browser-legal local closure uses 4000/4002/4008; protocol errors remain terminal local code 1002. Public close arguments are validated before state mutation, and retry jitter/timer ranges cannot overflow into immediate retries. No server wire-frame or schema change is needed.

`npm ci`, `npm run check`, `npm test` (**54 tests**), `npm run pack:check`, and `npm audit --omit=dev` pass locally; the audit reports no vulnerabilities. `python scripts/smoke_typescript_client.py` passes a real loopback server/native Node WebSocket journey: ready/history/activation, controlled transport closure, paused credential refresh, another authenticated client's offline creates/edits, recovered history/tombstones and resumed live delivery. Fake-clock tests cover flapping, timeout phases, stable recovery, stale callbacks/timers, failed sends/closes, reentrant listeners and validation. Old fixtures now complete history/activation and assert the revised browser-legal close code; their original assertions remain. Ruff lint/format, mypy and diff checks pass. Final package/CI and merge evidence will be recorded on the PR.

The [SDK migration contract](../clients/typescript/README.md#reconnect-behavior) documents the stronger `connect()` promise, attempt-local rejection while automatic retries continue, explicit manual retry budgets and caller-owned history reconciliation. SDK 0.4.0 changes only package version/metadata, not Python 0.12.0 or dependencies. The [WebSocket standard](https://websockets.spec.whatwg.org/#dom-websocket-close) defines permitted client close codes and UTF-8 reason limits; [Node timer behavior](https://nodejs.org/api/timers.html#settimeoutcallback-delay-args) motivates bounding delays. Timers can run late in a suspended tab/event loop; a stable-open interval is not ongoing liveness proof. The SDK cannot abort arbitrary credential-provider work or force a broken injected transport to physically close. Tests do not claim a full browser matrix, kernel network failures or measured reconnect-storm/load behavior.

Review added another failing regression at `0c202a6`: a synchronous application-message send error left state `connected`. Such failures now throw a connection error and enter bounded recovery without replaying the ambiguously delivered payload. The regression explicitly asserts that the new transport sends only its activation ping, not the old message. The SDK suite now contains **55 tests**. Initial packed-artifact verification installed SDK 0.4.0 in a clean external consumer directory, resolved its package export, typechecked its declarations and passed the same real-server recovery journey using that installed module; final-head rebuilt artifacts and CI remain the merge gates.

A final ownership regression at `05d5ec6` showed that a throwing injected send could synchronously close/reconnect and then fail the new connection. The public send now captures its starting generation before invoking the transport; late failure only affects that generation. The expanded SDK suite has **56 tests**. Initial full CI `33402647230` passed all ten jobs, 480 Python tests and 54 SDK tests; final-head results and rebuilt artifact digests are recorded on PR #35 before merge.

Remaining P1 work, ordered: real-process update/delete and quota contention; killed webhook-worker recovery; combined lifecycle/outage/reconnect storms and measured live-lag/load/soak; PostgreSQL-native backup/PITR/restore/application rollback and deployment acceptance. P2 telemetry/caching stays behind operator demand. The product remains an embeddable private/support-room backend for host-application developers; its core journey is signed customer/agent access, persistent chat/history, moderation, webhooks and data lifecycle. SQLite is the supported single-instance alpha; PostgreSQL is an unreleased preview. Publication and production deployment remain owner-controlled. No new infrastructure, spending, telemetry, credentials, license or schema change.

### PR #34 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `4e4c769` (merged PR #33), all ten [post-merge CI jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33397311414) passing: **441 tests, two SQLite-inapplicable skips, 89.56% branch-inclusive coverage**. Final-head PR #33 run `33396996717` passed the same 441 tests at 89.58%; its rebuilt wheel/sdist, runtime-only install, metadata and installed-wheel journey checks passed, with digests recorded on the PR. The previous goal turn made verified implementation progress and closed the startup-contention slice, not the full v0.13 release.

PR #34 addresses the next observed P1: a healthy lease indefinitely protecting a stale relay backlog. Its test-only baseline `e3f388f` in [run 33398400428](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33398400428) failed both live count/age cases because stale payloads were dispatched instead of fencing. The implementation checks actual unread row count and first-unread creation age before each batch, using configurable positive bounds (10000 events / 30 seconds by default). A violation fails readiness/admission, closes local sockets with 1012, and only then rotates the exact owner claim to the logical committed head. A fixed recovery UUID across retries avoids skipping newer events after a lost commit reply; another generation cannot be overwritten. Application retained-gap recovery now shares that retry-safe operation.

Thirty-two new database-free tests cover constructor/settings bounds, environment-to-runtime wiring, count/age/gap recovery ordering, failed fencing, ambiguous replies, cancellation, and replacement ownership. Additional live PostgreSQL cases cover committed row counts despite sequence gaps, database-time age checks, retry cursor preservation, ownership rejection, retention progress, and a signed-member two-application acceptance journey with healthy-peer writes, 1012 closure, rejected admission, history reload and resumed fan-out. The application test uses deterministic ASGI barriers, not separate OS processes. Local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **381 tests, two inapplicable skips, 99 deselected**; targeted relay/runtime tests pass **79 tests**. Ruff lint/format, mypy and diff checks pass. No local PostgreSQL or Docker executable is available; live CI and final artifact verification remain required before merge, and their results will be recorded on PR #34.

These are pre-batch overload checks, not a hard delivery-latency or disk-growth bound: in-flight dispatch, new commits after a check, database interruption and lease expiry can delay recovery/retention. Sustained overload may repeatedly disconnect clients. [PostgreSQL sequence semantics](https://www.postgresql.org/docs/current/functions-sequence.html) motivate counting committed rows instead of sequence distance; [database clock semantics](https://www.postgresql.org/docs/current/functions-datetime.html#FUNCTIONS-DATETIME-CURRENT) motivate avoiding host-clock age calculations. No measured throughput/RSS, load/soak, or reconnect-storm claim is made.

Initial implementation [run 33399961270](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33399961270) at `0504c2a` passed **98 PostgreSQL tests** but caught one fixture error: the new acceptance test read `history.messages` instead of the actual `history.items` envelope. The fixture now uses `items` and exits the stale TestClient socket context immediately after observing 1012, acknowledging closure before awaiting server-side presence cleanup instead of relying on lease expiry. All readiness/admission, healthy-peer, history and resumed-delivery assertions remain. The initial wheel/sdist build, Twine validation, fresh runtime-only install, `pip check`, and installed-wheel HTTP/WebSocket/moderation/webhook/export/lifecycle/backup smoke passed. Corrected-head CI and rebuilt artifacts remain merge gates.

Remaining ordered P1 work: real-process update/delete, quota contention and webhook-worker crash recovery; combined lifecycle/outage/reconnect storms and measured live-lag/load/soak; deployment manifests and PostgreSQL-native backup/PITR/rollback. The TypeScript reconnect-attempt reset on `ready` still needs adversarial post-ready failure coverage before stronger automatic-recovery claims. P2 telemetry/caching remains behind operator demand. The primary product journey remains an embeddable private/support-room backend: authenticated customer/agent access, durable history, live conversation, moderation, webhooks and operator data lifecycle. SQLite stays supported single-instance v0.12; PostgreSQL stays an unreleased preview. No deployment, publication, spending, schema (PostgreSQL 8 / SQLite 5), dependency or license change.

### PR #33 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `7fd3f07` (merged PR #32), no open PRs, and all ten [post-merge CI jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33394866036) passing: **416 tests, two SQLite-inapplicable skips, 89.46% branch-inclusive coverage**. The previous goal turn made verified implementation progress. This increment addresses the observed startup DDL versus active-replica maintenance contention.

Four failing regressions demonstrated DDL execution on current/future schema startup and missing pool cleanup when pool opening or schema initialization was cancelled. Startup now serializes inspection under the existing transaction-scoped migration advisory lock, checks whether metadata exists, and reads its committed version without a row-update lock. A current version returns without DDL, backfills, or metadata/retention writes. A future version fails closed without trying DDL. Missing/older schemas retain transactional migration and only record the supported version at the end. Cancellation now closes the startup pool and propagates without publishing the open state. Readiness requires exact schema equality instead of accepting a newer unsupported version. Schema numbers remain PostgreSQL 8 / SQLite 5.

Local `python -m pytest -q tests/test_postgres_schema_startup.py tests/test_postgres_deadline.py --timeout=20 --tb=short` passes **28 tests**. Ruff lint/format, mypy and `git diff --check` pass. Eleven live cases cover concurrent application-table access locks, server-enforced read-only inspection, unchanged metadata/retention rows across starts, blocked migration rollback and retry, advisory-lock waiting with a concurrent version change under default/serializable isolation, transaction-local isolation override, actual pool cleanup after cancelled lock waiting, and an existing WebSocket across three replica starts. Historical v2/v6 migration and concurrent empty-schema tests remain required gates. These are controlled PostgreSQL/ASGI tests, not a load benchmark or proof of online schema upgrades. No local PostgreSQL/Docker executable is available; full local and live CI, final package evidence and merge results will be recorded on the PR.

Initial [PR #33 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33396413923) at `89aabd2` passed eight jobs but the two database jobs caught an invalid test configuration: the application fixture used a maintenance interval below the supported 0.1-second minimum. Its corrected fixture keeps all continuity assertions. That run had **438 passes, two skips and one fixture failure**; the local non-PostgreSQL suite had **349 passes, two skips and 90 deselected**. Initial wheel/sdist build, Twine validation, runtime-only installation, `pip check` and installed-wheel HTTP/WebSocket/moderation/webhook/export/lifecycle/backup smoke passed.

Review then added a fifth failing regression: [run 33396662477](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33396662477) at `3866334` passed **90 PostgreSQL tests** but proved a serializable default could preserve a pre-lock snapshot and incorrectly accept a now-unsupported schema. Startup now explicitly selects Read Committed before obtaining its advisory lock, ensuring the subsequent version query sees the prior lock holder's commit. The choice is transaction-local, including with a read-only session default; it does not change later transaction settings or claim general application support for customized isolation. This follows PostgreSQL's [transaction snapshot semantics](https://www.postgresql.org/docs/current/transaction-iso.html). Final-head CI and rebuilt artifacts remain required before merge.

The schema marker is authoritative, not a physical-schema integrity audit or an automatic repair mechanism for manual DDL. Actual upgrades must drain/stop every old replica and background worker, take a PostgreSQL-native backup, initialize one new-version replica, verify it, then start matching replicas. Current-version restarts do not justify mixed-version rolling upgrades. PostgreSQL's [lock compatibility and deadlock behavior](https://www.postgresql.org/docs/current/explicit-locking.html) and [ALTER TABLE locking rules](https://www.postgresql.org/docs/current/sql-altertable.html) explain why `IF NOT EXISTS` alone did not make the previous startup path safe for live queries.

Remaining ordered P1 work: live-lag fencing; real-process update/delete, quota contention and webhook-worker crash recovery; combined lifecycle/outage/reconnect storms and measured load/soak; deployment manifests and PostgreSQL-native backup/PITR/rollback. Startup contention is not considered closed until its live regressions and final CI pass. Optional telemetry/caching remains P2 behind operator demand. SQLite stays the supported single-instance product and PostgreSQL stays an unreleased preview. No production deployment, package publication, paid resources, schema version, dependency or license change.

### PR #32 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `542dcee` (merged PR #31), no open PRs, and all ten [post-merge CI jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33391994996) passing: **343 tests, two SQLite-inapplicable skips, 89.26% branch-inclusive coverage**. The previous goal turn made verified implementation progress. This increment addresses the next P1: delayed pre-admission relay events overriding a later reconnect.

Four baseline failures demonstrated stale archive, ban, presence and frozen-room events reaching a newly admitted socket. Admission now returns the sequence of its own join event from the reservation transaction. Runtime registration retains that exclusive lower bound; the relay supplies event sequences out of band, and the manager filters under its shared registration/detachment lock. An event at or before admission cannot enqueue a snapshot or close that socket. Later events still reach older eligible sockets. Unsequenced local operations and global fences retain their existing behavior. This reuses the existing commit-order sequencer without a schema, query or lock addition; it does not sample wall clocks, the relay cursor, or a later database head.

Local verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **335 tests, two inapplicable skips, 80 deselected**. `python -m pytest -q tests/test_postgres_runtime.py tests/test_event_fence.py --timeout=30 --tb=short` passes **81 tests**. Coverage includes all eleven dispatched event types below/at/above admission, active/pending sockets, origin/room/subject matching, pre-admission backlog budget exclusion, registration-lock interleaving and invalid sequence values. The original presence test no longer accepts the previously delayed pre-admission join, and snapshot convergence now expects only the three post-admission mutations.

Six new live PostgreSQL cases use explicit relay barriers. Lifecycle writes originate from another real ASGI application replica; archive/reopen, archive/delete/recreate, ban/unban and freeze/unfreeze must preserve a later signed-member admission while still affecting older sockets. A presence case proves the old join is suppressed for the new peer while the new join reaches the existing peer. A final authorization control commits a ban between initial validation and reservation, holds relay dispatch, and requires the post-admission check to reject the member and release the physical lease. The join sequence is also compared directly with its persisted event record. These are real-storage/application tests, not independent OS-process fault injection. No local PostgreSQL/Docker executable is available.

Initial [PR #32 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33393843484) at `21f1187` passed all ten jobs: **415 tests, two skips, 89.33% branch-inclusive coverage**, including **80 live PostgreSQL tests**, the TypeScript journey and hardened container smoke. Local `python -m build --outdir dist/pr32-21f1187`, `python -m twine check dist/pr32-21f1187/*`, and a fresh runtime-only wheel installation with `pip check` and `scripts/smoke_installed_wheel.py` all passed; HTTP/search/WebSockets/read state/typing/moderation/webhooks/export/lifecycle/backup were exercised. The final authorization control was added after this run, so final-head CI and rebuilt artifacts remain required before merge. Exact final results and artifact digests are recorded on the PR; the third-party review integration skipped its draft review, not an independent approval.

The authorization control passed in [run 33394187826](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33394187826) at `9f78497`: all ten jobs passed, **416 tests, two skips, 89.46% coverage**, including **81 live PostgreSQL tests**. Rebuilt wheel/sdist validation and installed-wheel smoke also passed. Log review found a separate P1: startup's repeated `ALTER TABLE ... ADD COLUMN IF NOT EXISTS instance_generation` can deadlock with another replica's connection-reaping query. The dedicated PostgreSQL job logged a cycle between `AccessShareLock` and `AccessExclusiveLock`; PostgreSQL aborted maintenance and this run recovered. That is observed recovery, not proof that every startup/migration interleaving succeeds. Startup DDL versus active-replica contention needs a focused fix and regression tests before preview release. No new query/lock in this admission change caused that pre-existing DDL path. Final verification of this documentation-only follow-up is recorded on the PR.

Remaining ordered P1 work: startup migration/maintenance contention; live-lag fencing; real-process update/delete, quota contention and webhook-worker crash recovery; combined lifecycle/outage/reconnect storms and measured load/soak; deployment manifests and PostgreSQL-native backup/PITR/rollback. Post-admission events may still overlap later ready/history snapshots, and presence counts remain event-time snapshots. No durable per-client cursor, end-of-catch-up marker, or exactly-once delivery is claimed. Optional telemetry/caching remains P2 behind operator demand. SQLite is still the supported single-instance product; PostgreSQL stays an unreleased preview. No schema (PostgreSQL 8 / SQLite 5), dependency, license, production infrastructure, publication or paid-resource change.

### PR #31 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `d553eb9` (merged PR #30), no open PRs, and all ten [post-merge CI jobs](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33389393712) passing: **303 tests, two SQLite-inapplicable skips, 89.06% branch-inclusive coverage**. The previous goal turn made verified implementation progress. The current P1 is the ready/history activation gap seen in that turn's crash-test setup.

This increment starts with **seven failing regressions**: the manager discarded a pending presence event, and real SQLite/fake-PostgreSQL handlers dropped create/edit/delete broadcasts injected after a captured history snapshot, before ready, or just before activation. Registered pending sockets now retain immutable serialized snapshots, drain them after ready/history, and atomically enter live delivery only once the queue is empty. Arrivals during the drain join its tail; direct initial frames and the drain share each socket's operation lock. The queue is bounded at 64 events / 256 KiB per socket and 8 MiB aggregate by default, including in-flight activation payloads after detachment. The whole flush has one send-timeout deadline; overflow/failure/timeout attempts close 1013, and cancellation attempts 1012. Every detach path discards queued data. No payloads or private error details are logged.

Local verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. The final local `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **268 tests, two inapplicable skips, 75 deselected**. `python -m pytest -q tests/test_handshake_integration.py tests/test_handshake_buffer.py -m "not postgres" --timeout=30 --tb=short` passes **33 tests**, with one live PostgreSQL case deselected. The integration journey pauses actual history, commits HTTP create/edit/delete operations, then verifies history plus buffered mutations equals current durable rows, including tombstones. The shared PostgreSQL version must pass CI; no local PostgreSQL or Docker executable is available. Final-head CI, package evidence, and merge remain pending verification gates.

Review cases cover immutable snapshots, origin/room exclusion, arrivals during flush, event/UTF-8-byte/global overflow, budget reuse across rooms, all detachment paths, retained in-flight accounting, malformed embedded events, errors/cancellation/deadlines, a continuously replenished queue, and a repeatedly cancelled overflow producer. An additional failing deep-JSON regression exposed an unhandled encoder `RecursionError`; this now follows the same bounded close path. The focused buffer/storage integration command now passes **33 tests**, with one live PostgreSQL case deselected. Payload budgets are not process-RSS or sustainable-capacity claims. Initial history and delayed relay events can overlap; clients merge by ID and apply edits/tombstones rather than appending duplicates. This closes the local post-registration drop window, not rapid archive/reopen ordering, durable per-client replay, or an end-of-catch-up contract.

Initial [PR #31 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33390690812) at `2e7913a` passed the full quality job (**342 tests, two skips, 89.21% coverage**) and eight other jobs, including installed-wheel smoke. The dedicated PostgreSQL run exposed an old test assumption: Bob can now receive Alice's historical join (count 1) before his message because initialization buffers rather than drops it. That assertion now recognizes exactly that optional event before retaining the message comparison. This is not a fresh-count guarantee: presence counts are event-time snapshots, possibly older than `ready`. Obsolete relay snapshots are an explicit follow-up gate, not silently treated as current state. Final-head checks remain required after these refinements.

The follow-up Linux Python 3.14 run accepted deeper JSON than Windows, exposing a platform-dependent test assumption rather than a buffer failure. After reproducing the uncaught encoder exception on Windows, its regression now injects `RecursionError` directly instead of requiring a fixed nesting depth to exhaust every platform's C stack. The runtime retains the error handling; valid serializable nested events need not be rejected solely for their depth.

Remaining ordered P1 work: rapid lifecycle changes and obsolete presence/lifecycle snapshots from delayed relays; live-lag fencing; real-process update/delete, quota contention and webhook-worker crash recovery; kernel packet-loss/failover and measured load/reconnect storms; deployment manifests and PostgreSQL-native backup/PITR/rollback. The supported product remains single-instance SQLite and PostgreSQL remains an unreleased preview. Optional telemetry/caching remains P2 behind operator demand. This slice changes no schema (PostgreSQL 8 / SQLite 5), dependency, license, production infrastructure, package publication, or paid resource.

### PR #30 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `c6b810a` (merged PR #29), no open PRs, and all ten post-merge CI jobs passing: **282 tests, two SQLite-inapplicable skips, 88.87% branch-inclusive coverage**. The previous goal turn made verified implementation progress; this turn addresses the next P1, archive versus admission/lease-renewal ordering.

PR #30 starts with **four failing lifecycle cases and two passing storage/expiry controls** on that baseline. A room archived or deleted between validation and admission escaped the normal domain-error contract; a heartbeat observing an archived room before the relay incorrectly reported a storage outage. Admission and failed renewal now diagnose authoritative room state and produce archive (4409) or missing-room (4404) outcomes. Renewal also recognizes archived/deleted rooms after maintenance has reaped the reservation. Healthy renewal remains one query; active-room invalid/expired leases and unavailable storage retain the fail-closed 1012 behavior. An individual manager close can own its final event atomically, so heartbeat/room/member closers cannot duplicate that event or physical close.

The new live PostgreSQL tests deliberately change room state during admission and pause archive relay dispatch until heartbeat teardown wins, including reaped leases and room deletion. An unrelated room remains connected. These barriers exercise real PostgreSQL and ASGI handlers in one application process; they are not separate network-process fault tests. Existing separate-process normal-path moderation acceptance remains complementary evidence, not proof of all interleavings.

Local verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **229 tests, two inapplicable skips, 74 deselected**. Initial [PR #30 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33388072769) passed eight jobs but both database jobs exposed one invalid expired-lease fixture (SQLSTATE 23514): its expiry preceded its creation time. The fixture now moves both timestamps into the past while preserving the database constraint. The initial full run had **302 passes and two skips**, with that one failure; final-head CI is still a merge gate. No local PostgreSQL or Docker executable is available, so live verification runs in CI. Final CI and built-artifact evidence will be recorded on PR #30.

The corrected fixture passes all **74 PostgreSQL tests** in [run 33388584287](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33388584287), but its full quality job exposed an existing crash-test setup race: Bob could join before Alice was broadcast-active, losing the best-effort join event the test awaited. The crash/reap test now completes Alice's ready/history plus application ping/pong before connecting Bob. All join, message, crash-departure, count, and restart-history assertions remain; this is an explicit setup precondition, not a fix for activation-window delivery. The local handler/manager suite passes **59 tests, two inapplicable skips**. Final-head CI remains required; exact passing results and package digests are recorded on the PR before merge.

Remaining P1 work: ready/history activation-window convergence (now also observed in full CI); rapid archive/reopen with delayed events; live-lag fencing; real-process quota contention and webhook-worker crash recovery; kernel packet-loss/failover and measured load/reconnect storms; deployment manifests and PostgreSQL-native backup/PITR/rollback. The supported product remains single-instance SQLite, and PostgreSQL is an unreleased preview. P2 telemetry/caching stays behind demonstrated operator demand. This slice changes no schema (PostgreSQL 8 / SQLite 5), dependency, license, production infrastructure, package publication, or paid resource.

### PR #29 evidence (historical)

Latest verified baseline: clean `main...origin/main` at `42caa17` (merged PR #28), no open PRs, and all ten post-merge CI jobs passing (241 tests, 88.55% branch-inclusive coverage). The previous goal turn made verified implementation progress; the next P1 was authenticated-handshake resource ownership.

PR #29 starts from **22 failing handler cases** on that baseline (two PostgreSQL-only count cases are inapplicable to SQLite). The old handler registered sockets before its cleanup block and could strand local membership or database reservations when room/moderation rechecks, history, counts, or initial sends failed or were cancelled. Initial storage failures also bypassed the WebSocket error contract. The request now owns cleanup across the whole authenticated session, awaits background task termination and closure/release, preserves direct cancellation, and shields finalization from ASGI cancel scopes and repeated task cancellation. Raw pre-registration close is kept separate from manager-owned closure so a detached socket cannot be revived through a fallback send. Unexpected errors close 1011; translated storage errors close 1012 without database details.

Admission now attempts idempotent release when its commit result is unavailable or cancelled, but preserves existing ownership for known duplicate-ID rejection. A review regression first showed that indiscriminate error cleanup could release an existing duplicate owner; the narrowed error handling and dedicated test fix that. A second regression preserved SQLite `presence.left` after receive-loop storage failure by leaving physical closure and departure metadata with the finalizer. AnyIO is declared directly for cancellation scopes; it was already transitive through Starlette. No schema change (PostgreSQL 8 / SQLite 5), hosted service, production deployment, publication, or paid resource is involved.

Initial [PR #29 CI](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33385884619) at `c30fd93` passed all ten jobs: **277 tests, two inapplicable skips, 88.82% coverage**, including **63 PostgreSQL tests**. The live acceptance checks physical lease rows after injected post-reservation failures, then reconnects; expired rows cannot hide behind active-count filtering. Additional pre-admission cancellation and departure-preservation refinements pass locally. A further red/green regression ensures cancelling an individual closer after detachment cannot abandon the physical close, including when request cleanup cancels a closing heartbeat. `python -m pytest -q tests/test_connection_manager.py tests/test_websocket_lifetime.py tests/test_websocket.py tests/test_postgres_runtime.py --timeout=30 --tb=short` passes **69 tests, two inapplicable skips**. Ruff lint, formatting, mypy and diff checks pass. Final-head full CI remains a merge gate. No local PostgreSQL or Docker executable is available; their verification runs in CI.

Final local verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. `python -m pytest -q -m "not postgres" --timeout=60 --tb=short` passes **219 tests**, with two SQLite-inapplicable count cases skipped and 63 live PostgreSQL tests deselected. CI and package artifact evidence for the final head are recorded on PR #29 before merge.

PR #29 left archive versus lease-renewal ordering for PR #30, alongside the remaining P1 gates above. Cancellation shielding cannot survive process kill or guarantee immediate release during a database outage; lease expiry remains the backstop.

### PR #28 evidence (historical)

PR #28 started from `eead4bc` (merged PR #27), whose main CI passed 233 tests with 88.45% coverage. PR #28 subsequently merged as `42caa17` and passed all ten final-head and post-merge jobs; its verification and artifact digests are recorded on the PR.

PR #28 (`92bc2c8` implementation) completes the normal-path real-process moderation acceptance journey. Two independent Uvicorn processes use signed room-scoped members, not operator credentials for member requests. Freeze/unfreeze and mute/unmute enforce writes, ban evicts the same subject on both replicas and denies reconnect until unban, and archive closes both replicas before reopen restores durable history. Unaffected peers and the same subject in an unrelated room remain connected and writable. No hosted application, login database, or metered provider was added: this strengthens the embeddable private/support-room backend's existing core journey.

The accompanying manager regression tests first produced **6 failures / 8 passes** on the baseline: detached sockets accepted sends through a fresh lock, stale broadcast snapshots could send after detachment, and duplicate registration replaced the original lock/room ownership. A separate strengthened assertion failed because send failure forgot the socket without attempting physical close. The fix detaches before close, requires the original live registration for sends, rejects duplicate active registration, and attempts a bounded code-1013 close after failed sends. An already-started send can finish before close; delivery is not upgraded to a durable guarantee. The embedding contract change is explicit in README and CHANGELOG.

Local final implementation verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. `python -m pytest -q tests/test_connection_manager.py tests/test_conversation_controls.py tests/test_postgres_runtime.py --timeout=30` passes **32 tests**; `python -m pytest -q -m "not postgres" --timeout=60` passes **180 tests**, with 61 deselected. [PR #28 CI run 33383479399](https://github.com/Deathcharge/samsarix-chat-engine/actions/runs/33383479399) passes all ten jobs at `92bc2c8`: **241 tests, 88.55% branch-inclusive coverage**, including **61 PostgreSQL tests** in the dedicated Python 3.14/PostgreSQL 18.4 job. Python 3.10–3.14 Linux, Windows 3.12, package build/metadata/installed-wheel smoke, dependency audit, TypeScript-client checks, and container smoke all pass. The documentation follow-up must pass its own CI before merge. No local PostgreSQL or Docker executable is available, so live tests run in CI. No schema change (PostgreSQL 8 / SQLite 5), new dependency, production deployment, publication, or paid resource is involved.

PR #28's adversarial review queued handshake cleanup (addressed in PR #29) and archive versus lease-renewal ordering (addressed in PR #30). Its normal-path moderation tests alone did not prove either race fixed. See the [preview guide](POSTGRES_PREVIEW.md) and [acceptance checklist](MULTI_INSTANCE_ARCHITECTURE.md#release-acceptance-gates).

### Prior increment evidence

Merged PR #27 adds a separate checked-out PostgreSQL operation deadline, finite replacement-connect timeout, and per-session statement/idle-transaction limits. Deadline expiry closes the stalled session before interrupting its waiter, so Psycopg cancellation cannot wait indefinitely on the same transport. The fault proxy verifies silent bidirectional application-traffic stalls without cutting TCP, alongside reset/refusal, healthy-peer progress, readiness fencing, explicit reconnect/history recovery, and resumed delivery. A live query test proves a timed-out session is discarded and the single-connection pool recovers. The Python/OS matrix installs the PostgreSQL extra so database-independent deadline tests run throughout it; Python 3.10 explicitly skips overlapping-cancellation discrimination because it lacks cancellation-count APIs. Final CI passed 233 tests with 88.45% coverage. Timeouts do not prove rollback after a lost commit reply; [preview operations](POSTGRES_PREVIEW.md#operation-deadlines-and-ambiguous-outcomes) documents idempotent retry and reconciliation.

The supported v0.12 product remains the single-process SQLite backend described below. The guarded PostgreSQL preview now wires authoritative storage, leased coordination, rate controls, presence/typing, and the durable relay through the application. Merged PRs #23 (`9960ec9`) and #24 (`4c15537`) established the application runtime and real Uvicorn crash/restart test. The older baseline and release results below are historical evidence, not a claim that v0.13 has completed its release gates.

Merged PR #25 (`c8d77e7`) tests a database connection reset/refusal for one live replica while another remains available. It adds a local TCP fault proxy (test-only; no infrastructure mutation), explicit reconnect/history recovery checks, relay-claim readiness/admission checks, and best-effort lease cleanup that cannot skip pool closure. The reference test uses a three-second lease and a one-second pool timeout only for the interrupted replica; silently blackholed connections, database failover, live-lag fencing, load/soak, cross-process moderation/quotas/webhooks, deployment manifests, and verified PostgreSQL-native backup/PITR/rollback remain local engineering gates. See [preview operations](POSTGRES_PREVIEW.md) and the authoritative [release checklist](MULTI_INSTANCE_ARCHITECTURE.md#release-acceptance-gates).

2026-08-31 baseline: clean `main...origin/main` at `4c15537`, no open PRs, and no local PostgreSQL or Docker executable. Local verification of the network-recovery implementation: `ruff check .`, `ruff format`, `mypy samsarix_chat_engine`, and `git diff --check` pass; `pytest -q -m "not postgres" --timeout=60` passes **148 tests**, with 56 deselected. The three deterministic runtime failure tests pass without a database. Both process tests skip locally because `SAMSARIX_TEST_POSTGRES_URL` is unset; PR #25's dedicated PostgreSQL and full quality jobs are the live verification gates. This slice changes no database schema (PostgreSQL 8 / SQLite 5), no production infrastructure, and no paid-service dependency.

PR #25 final verification: all ten CI jobs passed at `318219c`, including live PostgreSQL and the full quality job (204 tests, 88.23% branch-inclusive coverage). A Python 3.14 matrix run exposed an existing test teardown race: leaving the Starlette socket context cancels its ASGI task before the orderly `presence.left` broadcast necessarily completes. The two-client test now explicitly disconnects Bob and observes his departure before context teardown, retaining the event assertions. The six WebSocket tests and 20 fresh-process repetitions of that disconnect scenario pass; the final Linux 3.10–3.14 and Windows 3.12 jobs also passed.

Late review of PR #25 identified an admission/fencing race: a database reservation could finish after local sockets were fenced, and then register a new socket during recovery. The follow-up binds admission to both the database generation and a monotonic local admission epoch, validates that token under the connection-manager lock shared with fencing, and releases reservations rejected by the guard or local capacity. Deterministic tests cover a fence during reservation, same-generation recovery before reservation completes, registration waiting on the shared lock, fencing after successful admission, capacity rejection, and a mismatched database generation. The test harness also restores normal pool timeouts outside the interrupted replica, drains child logs concurrently into a bounded tail, and verifies a child can emit one million bytes without blocking. These changes add no database schema, runtime service, or paid dependency; their final-head CI is the follow-up merge gate.

Follow-up local verification: `python -m ruff check .`, `python -m ruff format --check .`, `python -m mypy samsarix_chat_engine`, and `git diff --check` pass. `python -m pytest -q -m "not postgres" --timeout=60` passes **155 tests**, with 57 deselected. The child-output test passes when invoked explicitly without a database. An isolated in-memory mutation that disables the registration guard causes the lock-interleaving regression test to fail as expected; no source files were changed by that probe. Live PostgreSQL acceptance still requires CI because no local database is available.

## Repository assessment

The repository was extracted from `helix-unified` in commit `007ec96` and then received generic tests, templates, license, and README changes. At audit start it had three implementation modules but no `__init__.py`. Two modules imported `apps.backend.*` directly; on this workstation those imports silently resolved to `C:\Users\Andrew\Helix\helix-unified`, taking about 23 seconds and hiding the undeclared private-repository dependency. In an independent environment the modules were not usable.

The 20 original tests only asserted `dict` and `MagicMock` fixtures. They never imported or executed production code. The three examples imported nonexistent `ChatServer` and `WebSocketManager` APIs. The README linked to missing docs and CI, used commands inconsistent with the layout, described an unrelated multi-agent ecosystem, and claimed “Production Ready.” Packaging required nonexistent `helix-hub-shared>=0.1.0`; `requirements.txt` pinned a large unrelated AI/database/Discord/Celery stack.

No pre-existing worktree changes were present. The starting branch was `main` at `8a26c3b`, matching `origin/main`. No local feature branches, tags, CI configuration, committed secrets, generated artifacts, or lockfile were present. Python library projects normally publish bounded dependency metadata rather than lock transitive application environments, so v0.2 uses a single PEP 621 source of truth and tests supported dependency ranges in CI.

## Baseline evidence

Commands run before implementation:

| Command | Actual baseline result |
| --- | --- |
| `git status --short --branch` | Clean `main...origin/main` |
| `python --version` | Python 3.11.9 |
| `python -m pytest` | 20 passed in 3.36s, but all were fixture-only tests |
| `python -m compileall -q helix_chat_engine examples` | Passed syntax compilation |
| `python -m build` | Failed: `No module named build.__main__` |
| `python -m pip install --dry-run --no-build-isolation -e .` | Failed: no distribution for `helix-hub-shared>=0.1.0` |
| import of `helix_chat_engine.web_chat_server` | Succeeded only after loading `apps.backend` from sibling `helix-unified`; not independent |

## Product definition

Samsarix Chat Engine is a self-hosted, single-instance room chat backend and embeddable FastAPI application. It gives Python developers a narrow, inspectable way to add authorized private rooms, durable text history, and live delivery to a product, support workflow, private community, internal tool, or prototype.

- Target user: a Python application team that already authenticates users and needs embedded room chat without adopting a full collaboration platform or operating Redis/Postgres.
- Primary use case: an operator creates a room, a host application issues a short-lived room token to its authenticated user, the user commits and receives messages, and reconnect recovers authorized history.
- Independent reason to exist: a small reusable service with no private repository dependency and a conventional protocol.
- Product form: installable `samsarix-chat-engine` Python package, `samsarix-chat` CLI, and ASGI app factory.
- Distribution: source checkout or Python wheel under MPL-2.0; no cloud resources are required.
- Sustainability: maintenance/support or commercial embedding can be offered, but no demand or willingness-to-pay is assumed. Default operation incurs no metered API cost.

## Decisions and current research

- Retain FastAPI because the repository already used it and current official FastAPI guidance directly supports WebSocket endpoints, dependencies, and WebSocket-specific close errors.
- Use modern `pyproject.toml` metadata and extras, matching the Python Packaging User Guide's current dependency/optional-dependency guidance. Tests use Starlette's current `httpx2` TestClient backend rather than its deprecated `httpx` fallback.
- Use standard-library SQLite instead of the copied SQLAlchemy/Postgres stack. It completes persistence with no service dependency and keeps local evaluation honest.
- Persist before broadcast and support idempotency. This makes an acknowledged HTTP message durable and makes ordinary client retry safe.
- Keep presence in memory and explicitly label it best effort. Current Centrifugo guidance likewise treats presence as additional state with cost and privacy implications; this product does not pretend to match Centrifugo's multi-node broker/recovery scope.
- Keep one process. SQLite plus an in-process connection manager is coherent at this scale; adding Redis merely for a scaling claim would expand operations and failure modes.
- Package that one-process boundary as a non-root, read-only-root container with a durable `/data` volume and mounted secret files. Docker's official [build guidance](https://docs.docker.com/build/building/best-practices/) supports multi-stage builds, `.dockerignore`, CI image tests, and numeric non-root users; [Compose secret guidance](https://docs.docker.com/compose/how-tos/use-secrets/) supports per-service file mounts instead of ordinary secret environment values. FastAPI's [container guidance](https://fastapi.tiangolo.com/deployment/docker/) supports exec-form shutdown and one application process per container.
- Defer multi-instance claims until storage and coordination are designed together. Redis documents Pub/Sub as [at-most-once](https://redis.io/docs/latest/develop/pubsub/), while Streams add persisted cursors/acknowledgment rather than an authoritative chat database. PostgreSQL documents `LISTEN/NOTIFY` as interprocess signaling over a [shared PostgreSQL database](https://www.postgresql.org/docs/current/sql-notify.html), with payload, transaction, queue, and startup-race constraints. None alone resolves this repository's SQLite ownership, migration/restore exclusion, webhook leadership, or distributed quotas.
- Adopt PostgreSQL—not multi-process SQLite—as the v0.13 multi-instance source of truth. SQLite now documents a WAL-reset race affecting versions through 3.51.2 under concurrent cross-thread/process writes and checkpoints, while WAL remains same-host-only with one writer. The accepted [multi-instance architecture](MULTI_INSTANCE_ARCHITECTURE.md) requires a transactional event log, notification-assisted replay, leases, global quotas, claimed webhook work, migration/maintenance leadership, gap fencing, and real multi-process failure tests before any scale claim.
- Keep identity ownership in the host application. v0.4 accepts a narrowly profiled signed assertion, derives sender identity from it, and enforces room/action authorization on every request and WebSocket publish.
- Let production hosts retain private signing authority. v0.12 accepts a bounded static public JWKS using only Ed25519/EdDSA or RSA-2048+/RS256, binds every key and token to an explicit algorithm and `kid`, and rejects token-controlled remote key locations. Static local configuration avoids JWKS URL SSRF, cache, refresh, and availability failure modes while still permitting overlapping-key rotation.
- Retain the shared API key only as an operator and compatibility credential. It is intentionally all-room and must not be distributed to ordinary browser clients.
- Require exact non-local browser origins even for authenticated deployments; credentials do not remove cross-site WebSocket-hijacking risk.
- Keep read state subject-scoped, monotonic, capacity-bounded, and self-erasable. Current unread counts exclude self-authored and deleted messages, while cursors survive ordinary message retention.
- Keep typing signals ephemeral, transition-only, separately rate-limited, and automatically expired. They are not persistence or audit data and remain best-effort within one process.

Current official product research checked on 2026-08-01 established the practical feature floor without changing the narrow product boundary: [Sendbird Chat](https://sendbird.com/docs/chat) documents channels, receipts, presence, reactions, files, threads, search, moderation, export, webhooks, and privacy controls; [Ably Chat](https://ably.com/chat) emphasizes support/community embeds plus rooms, presence, typing, reactions, edits, moderation, and receipts. [Centrifugo authorization](https://centrifugal.dev/docs/server/authentication) uses signed identities or application-proxy decisions and per-channel permissions, while its [recovery guidance](https://centrifugal.dev/docs/server/history_and_recovery) distinguishes broker recovery from the application's source of truth. These comparisons support the identity/authorization-first v0.4, the accountable data-lifecycle v0.5, conversation controls next, and broker work only after measured single-instance demand.

The v0.5 lifecycle contract follows [RFC 9110 DELETE semantics](https://www.rfc-editor.org/rfc/rfc9110.html): successful deletion returns 204, repeat requests remain safe with respect to final resource state, and confirmation is carried in a header rather than relying on undefined DELETE request-body semantics. The [OWASP Logging Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html) supports recording administrative data changes and exports while excluding access tokens, secrets, and sensitive content. Mattermost's [compliance export documentation](https://docs.mattermost.com/administration-guide/comply/compliance-export.html) reinforces versioned, reconstructable administrative export as a real deployment need without implying compliance. Backup uses SQLite's official [online backup API](https://sqlite.org/backup.html), and the CLI verifies each generated snapshot before atomic placement.

The security design follows the [OWASP API Security Top 10](https://owasp.org/www-project-api-security/) emphasis on object authorization, authentication, and bounded resource use; the [OWASP Authorization Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html) guidance to deny by default and validate every request; the [OWASP WebSocket Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/WebSocket_Security_Cheat_Sheet.html) guidance on Origin validation, message authorization, and avoiding URL tokens; and [RFC 8725](https://www.rfc-editor.org/info/rfc8725/) requirements for explicit JWT algorithms, issuer/audience validation, mutually exclusive token rules, and safe key selection. [RFC 7517](https://www.rfc-editor.org/rfc/rfc7517.html) defines the local JWK Set format. PyJWT 2.13 is the bounded JWT dependency and cryptography 46–49 is the optional asymmetric implementation range.

- Make Samsarix LLC the canonical owner identity in v0.3 while retaining the v0.2 import, command, environment, and database names as tested migration aliases.
- Use the unmodified MPL-2.0: file-level copyleft protects distributed changes to covered files and notice preservation while allowing the engine to be combined with separate proprietary files. AGPL-3.0 would cover network use more strongly but materially narrows embedding adoption; Apache-2.0 would permit closed downstream modifications.

Primary references checked on 2026-07-28: [FastAPI's official WebSocket documentation](https://fastapi.tiangolo.com/advanced/websockets/), the [Python Packaging User Guide](https://packaging.python.org/en/latest/guides/writing-pyproject-toml/), [Starlette's TestClient documentation](https://www.starlette.io/testclient/), FastAPI/Pydantic/HTTPX2 PyPI metadata, [Centrifugo's official channel documentation](https://centrifugal.dev/docs/server/channels), [Mozilla's MPL-2.0 FAQ](https://www.mozilla.org/MPL/2.0/FAQ/), the [official MPL-2.0 text](https://www.mozilla.org/MPL/2.0/), and the [GNU license overview](https://www.gnu.org/licenses/).

## Architecture and trust boundaries

Untrusted HTTP and WebSocket payloads enter FastAPI/Pydantic validation. A configured operator key grants administrative access; signed short-lived tokens bind an application subject to rooms and read/write permissions. The server, not the payload, chooses authenticated sender identity. The service commits validated messages to a local SQLite path and then sends them to the in-process room registry. When an operator explicitly configures a webhook, a separate worker sends selected committed event bodies to that one validated destination; otherwise the engine makes no application-level outbound requests.

Deployment owners control login, membership decisions, TLS/proxying, operator and signing secrets, allowed browser origins, filesystem permissions, backups, deletion obligations, and access to the SQLite file. Any client holding the operator key or signing secret can access every room. Multi-process deployment breaks real-time fan-out and per-process rate-limit accounting, so it is unsupported.

## Findings and disposition

### P0

- [x] Remove the nonexistent `helix-hub-shared` installation blocker.
- [x] Remove runtime imports of sibling/private `helix-unified` modules.
- [x] Replace nonexistent public APIs and non-runnable examples with a real package API and CLI.
- [x] Implement the room/message/reconnect path with persistence and failure contracts.
- [x] Replace mock-only tests with production integration coverage.
- [x] Correct false “production ready,” CI, documentation, and license claims in the README.
- [x] Replace the mismatched customized BSL text with the standard MPL-2.0 and align package metadata, copyright notices, company identity, and contact channels.

### P1

- [x] Validate inputs and return stable, non-leaking errors.
- [x] Add optional shared-key authentication and safe loopback CLI behavior.
- [x] Add connection, room, message-size, send-rate, frame-size, and retention bounds.
- [x] Add idempotency, SQLite transactions/WAL/busy timeout, readiness, and graceful WebSocket shutdown.
- [x] Add Linux/Windows CI, lint, format, typing, coverage, build, and package checks.
- [x] Accurately document security, privacy, cost, recovery, and single-process behavior.
- [x] Add host-asserted user identity and server-side per-room authorization before use with mutually untrusted users.
- [x] Add room/message export and deletion administration for deployments with data-subject or retention obligations.
- [x] Add transactionally durable, signed application webhooks with bounded retries, operator recovery, and explicit receiver/network/privacy contracts.
- [x] Add a hardened, single-replica container/Compose deployment with mounted secret files, persistent-volume recovery, and CI smoke.
- [x] Add verification-only asymmetric access tokens with bounded static public-key rotation.
- [x] Reject sends after socket detachment and prove normal-path signed-member moderation/archive teardown across real PostgreSQL processes.
- [x] Cover authenticated-handshake failure/cancellation cleanup, repeated cancellation, ambiguous reservation release, and physical live-database lease cleanup/reconnect.
- [x] Diagnose archived/deleted rooms during admission and heartbeat renewal, including reaped leases and a delayed archive relay; prevent duplicate final notifications across competing closers.
- [x] Replace dropped post-registration initialization broadcasts with bounded ready/history-to-live queues, fail-closed overflow/deadlines, and storage/handler convergence tests.
- [x] Fence pre-admission relay events with a committed join sequence and exercise delayed lifecycle/presence ordering across real ASGI replicas.
- [ ] Cover combined lifecycle changes, OS-process outages, live lag and reconnect storms; controlled relay barriers alone do not prove these combinations.
- [ ] Complete the PostgreSQL release gates before multiple workers or hosts are supported; shared storage and durable relay/presence are implemented, while fault/lag/contended-quota/webhook-crash acceptance remains.
- [ ] Run sustained concurrent load/soak tests and publish measured limits before capacity claims.

### P2

- [x] Add explicit room archive/reopen lifecycle state.
- [x] Add a small framework-neutral TypeScript protocol client.
- [x] Add time-based retention in addition to count-based caps.
- [ ] Add OpenTelemetry hooks only if operators demonstrate a need; keep telemetry off by default.
- [ ] Add conditional HTTP caching/ETags for room lists if read load warrants it.

## Implementation checklist

- [x] PEP 621 package with console entry point and bounded runtime dependencies.
- [x] App factory and environment configuration with early validation.
- [x] SQLite rooms/messages, cursor history, idempotency, and retention.
- [x] HTTP room/message API and health/readiness.
- [x] WebSocket auth, ready/history, broadcast, presence, ping, validation, and close behavior.
- [x] Exact quick start, API reference, runnable examples, and contribution commands.
- [x] Real unit/integration/CLI/package-oriented tests and CI.
- [x] Final clean-environment verification and adversarial review.
- [x] Strict signed access tokens, operator separation, per-room/action authorization, and server-enforced sender identity.
- [x] Streaming room export, archive/reopen, confirmed deletion, age retention, metadata-only audit, and backup/restore.
- [x] Standard Webhooks-compatible committed-event outbox, delivery worker, health/replay API, rotation, and failure runbook.
- [x] Multi-stage non-root image, read-only-root Compose profile, secret files, persistent SQLite volume, and container CI gate.

## Release acceptance criteria

- Fresh install from the built wheel succeeds without another repository.
- `samsarix-chat --help`, `--version`, and loopback startup work; the deprecated `helix-chat` alias remains functional.
- The documented room → message → WebSocket → reconnect journey passes end to end.
- Lint, format, type check, tests with the configured coverage floor, build, and wheel metadata checks pass.
- No locally actionable P0 remains.
- Documentation contains no private-infrastructure requirement or unimplemented capability claim.
- License text, source notices, copyright identity, and wheel metadata all agree on MPL-2.0 and Samsarix LLC.

## Completed work

The copied multi-agent/UCF/Discord/Redis/LLM implementation was removed because it was not an independent product and every meaningful branch depended on `helix-unified`. It was replaced with a deliberately smaller FastAPI/SQLite service, safe CLI, stable protocol, bounded resource behavior, real test suite, CI, examples, and aligned documentation.

The adversarial pass additionally found and fixed a SQLite connection-handle leak, unbounded streamed HTTP request bodies, a WebSocket cancellation race exposed by the current Starlette/HTTPX2 backend, stale license metadata, a dead per-message `Location` link, and an inefficient retention query. The transport and persistence limits now have direct tests.

Version 0.3 completed the Helix-to-Samsarix product migration. The distribution, canonical Python package, CLI, environment variables, service metadata, documentation, support policy, and examples now use Samsarix. Compatibility shims preserve v0.2 imports and the old CLI/environment names, and the default database migration logic avoids silently hiding an existing `data/helix-chat.db`.

Version 0.4 establishes the product's first credible multi-user trust boundary. Host applications can issue short-lived room tokens through Python or the CLI; HTTP and WebSocket handlers validate token type, fixed algorithm, issuer, audience, required time/identity/authorization claims, maximum lifetime, and room/action access. Signed identity overrides display-name input, read-only WebSockets cannot publish, non-local browser origins require an explicit allowlist, and the administrative API key remains backwards compatible.

Version 0.5 closes the primary operational privacy gap for controlled single-instance deployments. Operators can stream versioned NDJSON exports, archive and reopen rooms, and irreversibly delete only an already archived room with an exact confirmation header. Archive is enforced at persistence and protocol layers and deterministically notifies/closes connected clients. Optional age retention complements count caps; a bounded administrative audit trail records actors and lifecycle metadata without duplicating message bodies or credentials. The CLI now creates integrity-checked online SQLite backups and atomically restores them with explicit replacement. Schema version 1 migrates in place to version 2, while unknown future versions are refused without mutation.

Version 0.6 supplies the conversation-control layer needed by embedded support, education, private-community, and live-event products. Signed authors can edit or tombstone their own messages; administrators can moderate any message. Room freeze preserves connected readers while reserving writes for administrators. Relative mute and ban controls bind to stable token subjects, with mute preserving reads and ban immediately evicting matching live sockets. Current message state survives reconnect, and audit records only message IDs and moderation metadata. Schema versions 1 and 2 migrate in place to version 3. The design is grounded in the analogous primitives documented by [Sendbird](https://docs.sendbird.com/docs/chat/platform-api/v3/moderation/moderation-overview), [Discord](https://docs.discord.com/developers/resources/message), and [Ably](https://ably.com/docs/chat/rooms/messages).

Version 0.7 reduces adoption friction with a checked-in, framework-neutral TypeScript client. It uses zero runtime dependencies, generated declarations, explicit ESM exports, injected web-standard transports, stable API errors, typed protocol events, first-message authentication, async credential refresh, and bounded reconnect state. The package shape follows [TypeScript's bundled-declaration guidance](https://www.typescriptlang.org/docs/handbook/declaration-files/publishing.html) and [Node's explicit-exports guidance](https://nodejs.org/api/packages.html); the reconnect observer surface reflects the connection states exposed by mature chat SDKs such as [Sendbird](https://sendbird.com/docs/chat/sdk/v4/javascript/event-handler/managing-connection-event-handlers/add-or-remove-a-connection-event-handler). The npm artifact is buildable and verified but remains unpublished until the owner chooses a package namespace/release gate.

Version 0.8 turns those primitives into an explicit support-room workflow. Signed users receive a persistent, non-regressing per-room cursor and a current unread count that excludes their own and deleted messages. They can remove their own state, while a per-room cap bounds storage. WebSocket writers can emit transient typing transitions under an independent limiter; starts refresh an advertised server deadline and stops occur on explicit command, successful publish, disconnect, or timeout without persistence or audit. The TypeScript client covers both contracts, and a runnable two-party support example demonstrates customer-to-agent-to-customer read state. This shape is grounded in [Stream's unread-state model](https://getstream.io/chat/docs/javascript/unread/) and Sendbird's [channel](https://sendbird.com/docs/chat/sdk/v4/javascript/channel/overview-channel) and [message](https://sendbird.com/docs/chat/sdk/v4/javascript/message/overview-message) guidance. It establishes the application state consumed by the separate v0.9 reliability milestone.

Version 0.9 closes that host-application integration gap with a SQLite transactional outbox for selected committed message and moderation events. It follows the [Standard Webhooks specification](https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md) for the stable ID, per-attempt timestamp, exact-body HMAC-SHA256 signature, rotation list, receiver idempotency, retry, timeout, HTTPS, and SSRF considerations. The single worker provides restart-safe at-least-once delivery, a bounded multi-day schedule with jitter and `Retry-After`, metadata-only health pagination, and manual replay. GitHub's [validation guidance](https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries) supports raw-body constant-time HMAC verification, while its [webhook practices](https://docs.github.com/en/webhooks/using-webhooks/best-practices-for-using-webhooks) support HTTPS, fast acknowledgement, and stable delivery-ID replay protection. Stripe's [delivery behavior](https://docs.stripe.com/webhooks#event-delivery-behaviors) reinforces automatic retries and the absence of an ordering guarantee. The implementation deliberately rejects redirect/query-secret destinations, blocks non-public resolution by default, pins the validated address for the connection, retains no receiver body, scrubs related payload copies on message/room deletion and age/count retention, and fails a mutation rather than silently dropping its event only when an all-pending outbox is full.

Version 0.10 adds retrieval against the named support-case journey rather than prematurely attempting generic collaboration search. Official [Sendbird message-search guidance](https://docs.sendbird.com/docs/chat/platform-api/v3/message/message-search/message-search-overview) and [Stream search guidance](https://getstream.io/chat/docs/php/search/) establish channel-scoped retrieval as a practical chat capability; [Ably's external-storage guidance](https://ably.com/docs/chat/external-storage-and-processing/data-storage) also makes the boundary clear when an application needs a separately governed long-term index. Samsarix therefore supplies authorized, Unicode-normalized substring matching over only one room's current retained messages, with cursor pagination and a separate limiter. The existing per-room history cap bounds a linear SQLite scan, schema 5 remains unchanged, and global/fuzzy/ranked search remains an explicit external-system decision.

Final v0.10 local verification on 2026-08-02 used Node 24.12.0, CPython 3.11.9 for the declared development environment, and CPython 3.14.6 for the clean installed artifact:

The initial verified implementation is commit `84dfecea8114a2e65e4ac4df1f936e1211eb2926`; Unicode/pagination review hardening and the final artifact surface are commit `4b1456dd1a655a173fc80eca0ad358a37e93cf52`.

| Gate | Result |
| --- | --- |
| Ruff lint / format, mypy, compile, diff check | Passed |
| Python integration and branch coverage | 105 passed; 88.66% total branch coverage, above the 85% gate |
| Python dependency audit | No known third-party vulnerabilities; unpublished local project distributions were skipped by registry lookup |
| TypeScript check / Node test runner | Passed; strict declaration build and 18/18 fake-transport/API tests |
| TypeScript audit / package inspection | Zero runtime dependencies, no known vulnerabilities, and 23 intended artifact files |
| real TypeScript integration smoke | Authenticated HTTP, search/edit convergence, read state, WebSocket auth/history/publish, reconnect, edit, delete, and tombstone recovery passed |
| Python build / Twine | Wheel and source distribution built in isolation and both passed metadata checks |
| clean Python 3.14 wheel install | Version 0.10.0 resolved from `site-packages`; HTTP, search, WebSocket, read state, typing, controls, seven exact-body signed webhooks, export, lifecycle, backup, and graceful shutdown passed |

Artifact SHA-256 digests:

- wheel: `6B837826522E3708ECE1AA51118B46FC9F64C45ADF14FD91BD845E64631AB239`
- source distribution: `7235B3EBE89DD88FD078078038FE5A672417F510949824A77E473296ED678C1F`

No database migration is introduced: v0.10 remains on schema 5. A rollback to v0.9 requires stopping v0.10, removing the optional search-rate environment variable, and starting v0.9 against the same database. Search queries may be copied into ordinary proxy access logs, so those logs remain part of the room-content privacy boundary. Sustained search load, multi-process behavior, fuzzy/global ranking, and an external security assessment were not claimed or run. If measured room size or query concurrency outgrows bounded scans, the next search-specific design gate is a reviewed SQLite FTS5 table or a write-maintained normalized index—not a silent scale claim.

Final v0.9 local verification on 2026-08-02 used Node 24.12.0, CPython 3.11.9 for the declared development environment, and CPython 3.14.6 for the clean installed artifact:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 49 Python/Markdown files formatted |
| `mypy samsarix_chat_engine scripts` / `compileall` / `git diff --check` | Passed; no type issues in 12 source/script files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 101 passed in 105.07s; 88.41% total branch coverage; no warnings |
| TypeScript `check` / Node test runner | Passed; strict declaration build and 17/17 fake-transport/API tests |
| TypeScript audit / package inspection | Zero runtime dependencies, no known vulnerabilities, and 23 intended artifact files |
| Python sdist / wheel / `twine check` | Wheel built through the sdist; webhook docs, source, smoke, tests, license, and notice included; both metadata checks passed |
| clean Python 3.14 wheel install | Version 0.9.0 resolved from `site-packages`; Samsarix LLC/contact/support/MPL metadata and declared dependencies passed |
| expanded installed-wheel smoke | HTTP, WebSocket, read state, typing, controls, seven exact-body signed webhooks, export, lifecycle, backup, and graceful shutdown passed |
| source/runtime dependency audits | No known third-party vulnerabilities; the unpublished local Samsarix distribution was explicitly reported as unauditable |

Exact artifact digests are recorded in the pull request because embedding them in packaged source would change those digests. The GitHub matrix remains the cross-platform merge gate; artifact digests will change if review fixes alter the source.

Final v0.8 local verification on 2026-08-01 used Node 24.12.0, CPython 3.11.9 for the declared development environment, and CPython 3.14.6 for the clean installed artifact:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 46 Python files formatted |
| `mypy samsarix_chat_engine` / `compileall` / `git diff --check` | Passed; no type issues in 9 source files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 89 passed in 93.99s; 90.94% total branch coverage; no warnings |
| TypeScript `check` / Node test runner | Passed; strict declaration build and 17/17 fake-transport/API tests |
| TypeScript production audit / package inspection | Zero runtime dependencies, no known vulnerabilities, and 23 intended artifact files |
| real TypeScript integration smoke | Authenticated HTTP, read state, first-message WebSocket auth, history, publish, reconnect, edit, delete, and tombstone recovery passed |
| Python sdist / wheel / `twine check` | Wheel built from the 81-entry sdist; 24 wheel entries; both metadata checks passed |
| clean npm and wheel installs | ESM/type import passed; installed wheel resolved from `site-packages` and passed HTTP, WebSocket, read-state, typing, controls, export, lifecycle, and backup smoke |
| source/runtime dependency audits | No known third-party vulnerabilities; unpublished local Samsarix distributions were explicitly reported as unauditable |

Exact Python sdist, Python wheel, and npm tarball digests are recorded in the pull request because embedding them in packaged source would change the source archive digest.

Final v0.7 local verification on 2026-08-01 used Node 24.12.0 and CPython 3.14.6, including clean installs of both built artifacts:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 30 Python files formatted |
| `mypy samsarix_chat_engine` / `compileall` | Passed; no issues in 9 source files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 82 passed in 27.17s; 91.20% total branch coverage |
| TypeScript `check` / Node test runner | Passed; strict declaration build and 15/15 fake-transport/API tests |
| TypeScript production audit / package inspection | Zero runtime dependencies, no known vulnerabilities, and 23 intended artifact files |
| real TypeScript integration smoke | Authenticated HTTP, first-message WebSocket auth, history, publish, edit, delete, and tombstone recovery passed |
| Python sdist / wheel / `twine check` | Wheel built from the 78-entry sdist; client source included, generated/dependency trees excluded, and both metadata checks passed |
| clean npm and wheel installs | ESM/type package import and expanded installed-wheel HTTP/WebSocket/lifecycle/backup smoke passed |

Exact Python sdist, Python wheel, and npm tarball digests are recorded in the pull request because embedding them in packaged source would change the source archive digest.

Final v0.6 local verification on 2026-08-01 used CPython 3.11.9 for the source suite and a clean CPython 3.14.6 environment for the installed artifact:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 41 Python files formatted |
| `mypy samsarix_chat_engine` / `compileall` / `git diff --check` | Passed; no type issues in 9 source files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 82 passed in 136.80s; 91.20% total branch coverage |
| fresh wheel-runtime `pip check` / `pip-audit --path` | No broken requirements; no known third-party vulnerabilities |
| sdist build / wheel rebuilt from sdist / `twine check` | Both 0.6.0 artifacts built and passed metadata checks |
| final wheel installed outside the source tree | Version 0.6.0, Samsarix LLC/contact metadata, dependency resolution, and imports passed |
| expanded installed-wheel smoke | HTTP/WebSocket persistence, edit/delete, freeze, mute/clear, export schema 2, archive/delete, backup, and graceful shutdown passed |

Exact artifact digests are recorded with the pull request because embedding them in the packaged source would change those digests. The GitHub matrix remains the cross-platform merge gate; artifact digests will change if review fixes alter the source.

Initial v0.5 verification on 2026-08-01 used CPython 3.14.6 on Windows and the newest resolved runtime versions inside the declared bounds:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 28 Python files formatted |
| `mypy samsarix_chat_engine` | Passed; no issues in 9 source files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 72 passed in 46.89s; 91.47% total branch coverage |
| `pip check` / isolated wheel-runtime `pip-audit` | No broken requirements; no known third-party vulnerabilities |
| `python -m build` / `twine check` | Source archive and universal wheel built from the sdist; both passed metadata checks |
| final wheel installed outside the source tree | Version/import/metadata and `pip check` passed |
| expanded installed-wheel smoke | Authorized HTTP/WebSocket persistence plus NDJSON export, archive/delete, online backup, SQLite creation, and graceful shutdown passed |

The lifecycle tests cover admin authorization, audit-content exclusion, idempotent archive, read-only enforcement, active-client teardown/reopen, confirmation failures, 1005-message streaming across batches, explicit and automatic retention counts, v1 migration/data preservation, future-schema refusal, cross-process restore exclusion, stale-sidecar removal, and Windows-safe backup/restore replacement. Exact final artifact digests are recorded in the pull request because including them in the packaged source would change those digests.

Initial v0.4 verification on 2026-08-01: the unchanged v0.3 baseline had 27 passing tests; after implementation and review hardening, `pytest --cov=samsarix_chat_engine --cov-report=term-missing` passed 59 tests in 33.02 seconds with 92.86% branch coverage. The new authorization tests cover tampering, expiry, issuer/audience confusion, malformed signed claims, self-verifiable token size, room and action denial, sender spoofing, subject-wide WebSocket rate limits, OpenAPI security schemes, browser WebSocket authentication, read-only sessions, origin enforcement, and CLI issuance. Final artifact and installed-wheel evidence is recorded after exact-head verification.

Final v0.4 local verification used CPython 3.11.9 and the newest resolved versions inside the declared bounds, including FastAPI 0.141.1, Uvicorn 0.52.1, PyJWT 2.13.0, and WebSockets 17.0.1:

| Check | Actual result |
| --- | --- |
| `ruff check .` / `ruff format --check .` | Passed; 36 Python files formatted |
| `mypy samsarix_chat_engine` | Passed; no issues in 9 source files |
| `pytest --cov=samsarix_chat_engine --cov-report=term-missing` | 59 passed in 33.02s; 92.86% total branch coverage |
| `pip check` | No broken requirements |
| `pip-audit` | No known third-party vulnerabilities; unpublished local distributions skipped |
| `python -m build` / `twine check` | Final sdist and universal wheel built from the sdist; both passed metadata checks |
| final wheel installed outside the source tree | Version/import/metadata and `pip check` passed |
| `scripts/smoke_installed_wheel.py` under final wheel runtime | Real operator room creation, token issuance, authenticated HTTP persistence/history, browser-style WebSocket auth/recovery/publish, SQLite creation, and graceful shutdown passed |

Exact artifact digests are recorded with the pull request because embedding a digest inside its own source archive is self-referential. The GitHub Actions matrix, external security review, and sustained load/soak tests were not run locally and remain named gates rather than implied evidence.

## Version 0.2 verification evidence

All source checks below ran in a newly created `.venv-productization` environment installed only from `.[dev]`. That install resolved FastAPI 0.140.7, Starlette 1.3.1, Pydantic 2.13.4, Uvicorn 0.51.0, HTTPX2 2.9.1, and the declared quality tools without a resolver error.

| Command | Actual result |
| --- | --- |
| `python -m ruff check .` | Passed |
| `python -m ruff format --check .` | Passed; 22 files already formatted |
| `python -m mypy helix_chat_engine` | Passed; no issues in 8 source files with the Python 3.10 target |
| `python -m pip_audit` | No known vulnerabilities found; local package skipped because it is not on PyPI |
| `python -m pytest --cov=helix_chat_engine --cov-report=term-missing` | 23 passed in 69.93s; 91.33% total branch coverage; no warnings |
| `python -m build` | Built sdist and universal wheel without metadata warnings |
| `python -m twine check dist/*` | Both artifacts passed |
| `python -m compileall -q helix_chat_engine examples` | Passed |
| `helix-chat --version` / `helix-chat serve --help` | Returned version 0.2.0 and documented options |
| five repeated multi-client WebSocket tests | 5/5 passed after the cancellation fix |

A second `.venv-runtime` environment installed only the built wheel and runtime dependencies. From `.venv-runtime/smoke` (outside the source directory), `pip check` reported no broken requirements and `helix_chat_engine.__file__` resolved inside `site-packages`. The wheel's CLI started on loopback, `/readyz` returned ready, an HTTP room/message round trip returned persisted content, and `examples/02_websocket_chat.py` received `ready`, recovered history, and a committed `message.created` event. The earlier source-install smoke also ran both shipped REST and WebSocket examples successfully. Ctrl+C stopped both test servers and released their listening ports.

The first advisory scan identified only the system Python's inherited `setuptools 65.5.0`. The build/dev floor and setup instructions were raised to `setuptools >=83`, both isolated environments were upgraded to 83.0.0, and repeated `pip-audit` checks reported no known vulnerabilities in either the full development environment or the wheel-only runtime path. The local `helix-chat-engine` package itself was explicitly skipped because it is not published on PyPI; Ruff security rules and direct threat review cover its source.

Locally available verification was Windows 10/11 with CPython 3.11.9. The configured GitHub Actions matrix for Linux CPython 3.10–3.14 and Windows CPython 3.12 was not executed locally and remains the normal pre-release merge gate. No sustained load/soak test, external security assessment, or multi-process test was run; those unsupported scopes are listed as P1 work rather than implied as passing.

## Version 0.3 verification evidence

The Samsarix migration was verified again from source and from the final wheel on 2026-07-28:

| Check | Actual result |
| --- | --- |
| `python -m ruff check .` / `ruff format --check .` | Passed; 31 Python files formatted |
| `python -m mypy samsarix_chat_engine` | Passed; no issues in 8 canonical source files |
| `pytest --cov=samsarix_chat_engine` | 27 passed in 19.68s; 91.70% total branch coverage |
| runtime-only `pip-audit` | No known vulnerabilities; unpublished local package skipped |
| `python -m build` / `twine check` | Source archive and universal wheel built and passed |
| `python -m compileall` / `pip check` / `git diff --check` | Passed |
| official MPL comparison | Local `LICENSE` matched Mozilla's official MPL-2.0 text after line-ending/trailing-space normalization |

The source archive contains the security policy, documentation, examples, license, notice, and both Python package names. A fresh `.venv-samsarix-runtime` installed only the final wheel and its runtime dependencies. Both commands returned 0.3.0, both imports resolved from `site-packages`, the legacy import emitted its deprecation warning, metadata reported `MPL-2.0` with `LICENSE` and `NOTICE`, and `pip check` passed. The installed `samsarix-chat` command then started on loopback, created a room and persisted message over real HTTP, returned that history, wrote its SQLite database, and shut down gracefully.

This v0.3 verification was local on Windows with CPython 3.11.9. The configured GitHub Actions matrix remains the cross-platform merge gate and had not run before the branch push.

## Deferred and blocked work

Binary upload/download hosting, object-store integration, media transformation, and end-user attachment lifecycle UI remain host-application concerns. Multi-instance capacity evidence on controlled infrastructure, longer sustained soak, and broader real-client failure injection remain genuine next-stage engineering, ordered in the roadmap. The current PostgreSQL fan-out, separated token-signing trust, reliable committed-event integration, and hardened container profile are complete enough for controlled application evaluation; capacity and availability claims remain intentionally bounded by the recorded measurements.

Public package publication, hosted deployment, domains, credentials, signing, and pricing remain owner-controlled. No external accounts, infrastructure, releases, or spending were created as part of the local productization work.

## Release disposition

**Alpha release candidate for controlled single-instance evaluation.** The developer product has no known locally actionable P0, and its source, package, authorized primary journey, limits, error states, documentation, brand identity, and standard open-source license are coherent. Production or regulated use still requires deployment-specific identity integration, lifecycle operations, capacity evidence, and external security review.

## Known risks

- The operator API key and optional HS256 token signing secret are high-impact symmetric credentials; prefer public-JWKS verification where practical.
- The webhook HMAC secrets authorize receiver trust and payloads duplicate selected plaintext content in the outbox/receiver. Each attempt validates resolution and pins the selected address while preserving TLS hostname verification, but deployment-level egress/routing controls remain necessary defense in depth.
- Access tokens have bounded expiry and optional asymmetric keys, but no per-token revocation list or automatic remote key refresh; JWKS changes require restart.
- SQLite and the in-process connection registry intentionally limit scale and topology.
- Count-based deletion has no audit log and is unsuitable where legal holds are required.
- Message content is plaintext at rest unless the deployment encrypts the filesystem.
- Presence is at-most-once and may be stale briefly around abrupt disconnects.
- In-memory rate limits reset on restart and are not coordinated across processes.
